# Completed Tasks Log — Dental Clinic Management System

> **Living document.** Updated every time a task is successfully completed.
> Current plan is in [plan.md](./plan.md).

---

## Completed Tasks

| # | Task | Completed At | Notes |
|---|------|-------------|-------|
| D-01 | Create PRD from FR/NFR requirements | 2026-08-29 12:25 UTC+08:00 | Created `docs/PRD.md` — 11 sections covering Overview, Core Features, Technical Stack, Data Requirements, Dependencies, Implementation Phases, UI/UX, Security, Constraints, Success Metrics, References |
| D-02 | Create Architecture Document from PRD | 2026-08-29 12:26 UTC+08:00 | Created `docs/ARCHITECTURE.md` — 13 sections covering system overview, high-level architecture, frontend/backend/database architecture, integration architecture, security architecture, realtime, deployment, workflow data flows, error handling, directory structure, design principles |
| D-03 | Create Security Document expanding PRD/Architecture | 2026-08-29 12:26 UTC+08:00 | Created `docs/SECURITY.md` — 19 sections covering STRIDE threat model, authentication, RBAC with full RLS policies, frontend/backend/database security, R2 storage security, Messenger integration security, QR code security, audit logging, RA 10173 compliance, secrets management, security headers, rate limiting, SDLC, incident response, Devin 5-subagent audit process, pre-deployment checklist |
| D-04 | Create plan.md and plan_done.md | 2026-08-29 12:26 UTC+08:00 | Created `docs/plan.md` (7 implementation phases, 70+ tasks) and `docs/plan_done.md` (this file) |
| D-05 | Create UI/UX Design Document | 2026-08-29 12:42 UTC+08:00 | Created `docs/UI_UX.md` — 17 sections: Product Analysis, UX Goals, User Personas (4), User Journey (3 flows), Information Architecture, Navigation (desktop/mobile/public), Screen-by-Screen Breakdown (14 screens including FDI dental chart), UX Decisions (Nielsen heuristics + cognitive psychology), UI Design Direction, Design System (colors, typography, spacing, grid, radius, shadows, all components), Accessibility (WCAG 2.2 AA), Microinteractions (20+), Motion Design, Edge Cases (15+ scenarios), Developer Notes, Future Improvements, UX Audit & Recommendations |
| D-06 | Create Development Order Document | 2026-08-29 12:50 UTC+08:00 | Created `docs/dev-order.md` — 7 phases, 42 ordered tasks each with QA checklist, files to create, reference links, dependency graph, and 30+ documentation links for AI development |
| D-07 | Create bug tracking document | 2026-08-29 12:52 UTC+08:00 | Created `docs/donetask_bug-encountered.md` — Bug log table (severity, status, repro steps, fix details), per-bug detail template, and summary statistics table |
| D-08 | Update all docs with correct library links | 2026-08-29 13:05 UTC+08:00 | Updated 5 docs (PRD, ARCHITECTURE, UI_UX, SECURITY, dev-order): QR lib → `qrcode.react` (https://www.npmjs.com/package/qrcode.react), signature lib → `react-signature-canvas` npm link (https://www.npmjs.com/package/react-signature-canvas), added Calendar.js (https://calendarjs.com/), added RA 8792 E-Commerce Act (https://www.gppb.gov.ph/wp-content/uploads/2023/06/Republic-Act-No.-8792.pdf), added PDA Dental Chart ref to SECURITY |
| P1-01 | Initialize Next.js 14+ project (App Router, TypeScript strict) | 2026-08-29 13:12 UTC+08:00 | Created Next.js 16.3.3 project with TypeScript strict, Tailwind CSS v4, App Router, src/ dir. Installed: shadcn/ui (base-nova style, 16 components: button, card, input, label, alert, dialog, dropdown-menu, sheet, sonner, badge, skeleton, avatar, separator, tabs, table, tooltip), lucide-react, @tanstack/react-query, react-hook-form, zod, @hookform/resolvers, date-fns, clsx, tailwind-merge, class-variance-authority. Dev server verified: GET / 200, page renders with shadcn Card + Button + Lucide icon. |
| P1-02 | Setup Tailwind CSS + shadcn/ui + Lucide icons | 2026-08-29 13:20 UTC+08:00 | Replaced Geist font with Inter (cv11, tnum features via CSS). Updated globals.css with full design system: primary blue (#2563EB), warm neutrals, 6 semantic pastel colors (success/warning/danger/info/purple/teal), tooth state colors, custom radius (6/8/12/16px), shadow scale (xs/sm/md/lg), dark mode tokens. Created `src/lib/constants/theme.ts` with TOOTH_STATES, BOOKING_STATUS_STYLES, VISIT_STATUS_STYLES, PAYMENT_STATUS_STYLES. Verified: page renders with Inter font, custom tokens, shadcn components. |
| P1-03 | Setup Supabase CLI local dev (init, Docker, config) | 2026-08-29 13:30 UTC+08:00 | Installed `supabase` as dev dependency. Ran `npx supabase init` (created `supabase/config.toml`). Started local stack with `npx supabase start` — all services running (Studio: http://127.0.0.1:54323, API: http://127.0.0.1:54321, DB: postgresql://postgres:postgres@127.0.0.1:54322/postgres). Installed `@supabase/supabase-js` and `@supabase/ssr`. Created `.env.local` with local credentials and `.env.example` for reference. |
| P1-04 | Create database schema migrations (all 21 tables, 3NF) | 2026-08-29 13:40 UTC+08:00 | Created migration `20260829052530_create_core_schema.sql` with: 8 enum types (user_role, booking_status, visit_status, payment_status, payment_method, block_type, recurrence_rule, conversation_status, message_direction), 21 tables (users, dentists, dentist_schedules, dentist_blocks, clinic_settings, clinic_holidays, patients, dental_services, appointments, appointment_services, appointment_history, qr_codes, consent_forms, treatment_records, invoices, payments, waitlist_entries, audit_logs, messenger_conversations, messenger_messages, reassignment_logs), all FK constraints, CHECK constraints, 17 indexes, updated_at trigger function + triggers on 14 tables, audit_log immutability triggers. Verified: `npx supabase db reset` succeeded, all 21 tables confirmed in database. |
| P1-05 | Create RLS policies for all tables | 2026-08-29 13:45 UTC+08:00 | Created migration `20260829052857_create_rls_policies.sql`. RLS enabled on all 21 tables. Helper functions: `get_user_role()`, `get_current_dentist_id()`. Policies per SECURITY §4.3: users (own/admin), dentists (all read), patients (non-archived read, reception/admin write), appointments (role-based with dentist filtering), consent_forms (dentist own + admin, INSERT only), treatment_records (dentist own + admin), audit_logs (admin read only, service role INSERT), messenger_* (reception/admin), immutable tables (appointment_history, messenger_messages, reassignment_logs — INSERT only). DELETE denied on most tables (archive instead). Verified: `npx supabase db reset` succeeded. |
| P1-06 | Create database triggers (audit, status validation, QR invalidation) | 2026-08-29 13:50 UTC+08:00 | Created migration `20260829053054_create_triggers.sql` with 5 triggers: (1) `validate_appointment_status` — BEFORE UPDATE on appointments, validates booking_status & visit_status transitions per ARCHITECTURE §5.4; (2) `log_appointment_history` — AFTER UPDATE on appointments, logs field changes to appointment_history; (3) `invalidate_qr_code` — BEFORE UPDATE on qr_codes, auto-sets used_at, prevents reuse; (4) `update_invoice_payment_status` — AFTER INSERT/UPDATE on payments, recalculates invoice + appointment payment_status (SECURITY DEFINER); (5) `generate_reference_no` — BEFORE INSERT on appointments, auto-generates unique reference_no. Verified: `npx supabase db reset` succeeded. |
| P1-07 | Create database indexes | 2026-08-29 13:40 UTC+08:00 | 17 indexes included in core schema migration (`20260829052530_create_core_schema.sql`): patients (name, contact, email), appointments (date+dentist, booking/visit/payment status, reference_no), qr_codes (token), audit_logs (entity, user+timestamp), messenger_conversations (psid, status), waitlist (date+joined), dentist_schedules (dentist+day), dentist_blocks (dentist+start). |
| P1-18 | Seed data for local development | 2026-08-29 13:55 UTC+08:00 | Created `supabase/seed.sql` with: 4 users (admin, reception, 2 dentists), 2 dentists with license_no and specialization, 9 dentist_schedules (Mon-Sat), 18 clinic_settings (general, schedule, booking, notifications, security), 5 clinic_holidays, 10 dental_services (checkup, extraction, filling, root canal, whitening, ortho, x-ray, crown, denture, fluoride), 8 patients with medical history and allergies. Verified: all counts confirmed via `npx supabase db query`. |
| P1-08 | Setup Supabase Auth (login, logout, session, JWT cookies) | 2026-08-29 14:00 UTC+08:00 | Created 4 Supabase client utilities: `src/lib/supabase/browser-client.ts` (createBrowserSupabaseClient using @supabase/ssr createBrowserClient), `src/lib/supabase/server-client.ts` (createServerSupabaseClient with next/headers cookies), `src/lib/supabase/middleware-client.ts` (updateSession function for middleware session refresh), `src/lib/supabase/admin-client.ts` (createServiceRoleClient using SUPABASE_SECRET_KEY). Created `src/middleware.ts` with matcher excluding static files. TypeScript compiles cleanly (`npx tsc --noEmit` — 0 errors). |
| P1-13 | Setup type system (lib/types/) | 2026-08-29 14:05 UTC+08:00 | Created `src/lib/types/enums.ts` (9 union types: UserRole, BookingStatus, VisitStatus, PaymentStatus, PaymentMethod, BlockType, RecurrenceRule, ConversationStatus, MessageDirection) and `src/lib/types/database.ts` (21 entity interfaces + Database type map for Supabase generic typing). |
| P1-14 | Setup Zod validation schemas (lib/validations/) | 2026-08-29 14:05 UTC+08:00 | Created `src/lib/validations/enums.ts` (9 Zod enum schemas), `patient.schema.ts` (patientSchema with PH phone regex, patientSearchSchema), `appointment.schema.ts` (create, update, search schemas), `index.ts` (loginSchema, dentistScheduleSchema, dentalServiceSchema, consentFormSchema, treatmentRecordSchema, paymentSchema, clinicSettingSchema). All schemas shared client/server for defense-in-depth. TypeScript compiles cleanly. |
| P1-12 | Setup service layer skeleton (lib/services/) | 2026-08-29 14:10 UTC+08:00 | Created `base-service.ts` (BaseService abstract class, ServiceError, PaginatedResult), `patient-service.ts` (CRUD + search + archive), `appointment-service.ts` (CRUD + search + archive + service linking), `index.ts` (AuthService, DentalServiceService, ClinicService). All services use Supabase client injected via constructor. TypeScript compiles cleanly. |
| P1-15 | Setup custom hooks skeleton (lib/hooks/) | 2026-08-29 14:15 UTC+08:00 | Created `use-auth.ts` (useAuth hook with auth state listener), `use-patients.ts` (usePatients, usePatient with TanStack Query), `use-appointments.ts` (useAppointments, useAppointment), `use-clinic.ts` (useDentalServices, useClinicSettings). All hooks use browser Supabase client + TanStack Query for caching. TypeScript compiles cleanly. |
| P1-09 | Implement RBAC route guards (Next.js middleware) | 2026-08-29 14:20 UTC+08:00 | Updated `src/middleware.ts` with: PUBLIC_ROUTES list (/login, /unauthorized), ROLE_ROUTES map (/settings→admin, /consultation→dentist, /consent→dentist), getUserRole function (queries users table for role), redirect to /login with redirect param if unauthenticated, redirect to /unauthorized if role mismatch. Session refresh via updateSession preserved. |
| P1-16 | Configure security headers in next.config.js | 2026-08-29 14:20 UTC+08:00 | Updated `next.config.ts` with security headers per SECURITY §14: Strict-Transport-Security (max-age=31536000; includeSubDomains; preload), X-Frame-Options (DENY), X-Content-Type-Options (nosniff), Referrer-Policy (strict-origin-when-cross-origin), Permissions-Policy (camera=(), microphone=(), geolocation=()), Content-Security-Policy (default-src self, script-src self+unsafe-inline+unsafe-eval, style-src self+unsafe-inline, img-src self+data+https, font-src self+data, connect-src self+supabase, frame-ancestors none, base-uri self, form-action self). |
| P1-10 | Create base layout (sidebar, topbar, mobile nav) | 2026-08-29 14:30 UTC+08:00 | Created `src/lib/constants/navigation.ts` (10 nav items with role-based visibility per UI_UX §6.1), `src/components/layout/sidebar-nav.tsx` (role-filtered sidebar with active state), `src/components/layout/topbar.tsx` (user dropdown with sign-out, mobile Sheet nav using base-ui render prop), `src/components/layout/dashboard-shell.tsx` (full layout wrapper with sidebar + topbar + content), `src/app/(dashboard)/layout.tsx` (server-side auth check + user fetch), `src/app/(dashboard)/dashboard/page.tsx` (placeholder overview with 4 stat cards). TypeScript compiles cleanly. |
| P1-11 | Create login page | 2026-08-29 14:30 UTC+08:00 | Created `src/app/(auth)/login/page.tsx` — client component with React Hook Form + Zod loginSchema validation, Supabase signInWithPassword, redirect param support, error display via Alert, loading state, branded card with Activity icon. Created `src/app/(auth)/unauthorized/page.tsx` — access denied page with ShieldAlert icon and link to dashboard. TypeScript compiles cleanly. |
| P2-04 | Dental services catalog CRUD | 2026-08-29 15:00 UTC+08:00 | Extended `DentalServiceService` with getAllServices, updateService, toggleActive methods. Added `ServiceResult<T>` type to base-service. Created `src/app/(dashboard)/services/actions.ts` (createServiceAction, updateServiceAction, toggleServiceActiveAction server actions with Zod validation). Created `src/components/services/service-form-dialog.tsx` (Dialog with RHF + Zod, create/edit mode). Created `src/components/services/service-list.tsx` (Table with dropdown actions: edit, activate/deactivate, empty state). Created `src/app/(dashboard)/services/page.tsx` + `services-client.tsx` (server fetch + client interactivity). Added /services to navigation (admin-only, Sparkles icon) and middleware ROLE_ROUTES. Installed shadcn textarea component. TypeScript compiles cleanly. |
| P2-01 | Patient CRUD service + hooks | 2026-08-29 15:15 UTC+08:00 | PatientService already had full CRUD (getPatients, getPatientById, createPatient, updatePatient, archivePatient) from P1-12 skeleton. Created `src/lib/hooks/use-debounce.ts` for debounced search. No additional service code needed. |
| P2-02 | Patient list page with search | 2026-08-29 15:20 UTC+08:00 | Created `src/app/(dashboard)/patients/actions.ts` (createPatientAction, updatePatientAction, archivePatientAction with Zod validation). Created `src/components/patients/patient-search.tsx` (debounced 300ms search with loading spinner). Created `src/components/patients/patient-form-dialog.tsx` (Dialog with RHF + Zod, PH phone regex validation, create/edit mode, medical history + allergies fields). Created `src/app/(dashboard)/patients/page.tsx` (server-side fetch with searchParams) + `patients-client.tsx` (client interactivity, table with dropdown actions, empty state). TypeScript compiles cleanly. |
| P2-03 | Patient detail page (record, history, dental chart) | 2026-08-29 15:20 UTC+08:00 | Created `src/app/(dashboard)/patients/[id]/page.tsx` (server-side fetch, notFound if missing) + `patient-detail-client.tsx` (tabbed interface: Profile, Medical History, Visit History, Billing; contact info cards with icons; allergies with warning icon; edit dialog; empty states for visits/billing). TypeScript compiles cleanly. |
| P2-03b | Dentist Schedule Management | 2026-08-29 15:40 UTC+08:00 | Created `src/lib/services/dentist-service.ts` (DentistService with getAllDentists, getDentistById, getSchedules, createSchedule, updateSchedule, deleteSchedule, getBlocks, createBlock, deleteBlock). Created `src/app/(dashboard)/dentists/[id]/schedule/actions.ts` (createScheduleAction, deleteScheduleAction, createBlockAction, deleteBlockAction). Created `src/components/dentists/schedule-form-dialog.tsx` (day/time selector with base-ui Select). Created `src/components/dentists/block-form-dialog.tsx` (datetime, block type, recurrence, reason). Created `src/app/(dashboard)/dentists/[id]/schedule/page.tsx` + `schedule-client.tsx` (working schedules table + time-off blocks table, delete actions, empty states). Installed shadcn select component. TypeScript compiles cleanly. |
| P2-05 | Appointment creation with triple status model | 2026-08-29 16:00 UTC+08:00 | Created `src/app/(dashboard)/appointments/actions.ts` (createAppointmentAction with Zod validation). Created `src/components/appointments/appointment-form.tsx` (patient/dentist Select dropdowns, date/time pickers, service multi-select with checkboxes, auto-duration calculation, validation, success/error alerts). Created `src/app/(dashboard)/appointments/new/page.tsx` (server-side fetch of patients, dentists, services). Triple status initialized via DB defaults (booking_status=pending, visit_status=null, payment_status=pending_payment). Installed shadcn checkbox component. TypeScript compiles cleanly. |
| P2-07 | Appointment duration calculation | 2026-08-29 16:00 UTC+08:00 | Duration auto-calculated client-side via useMemo from selected services' default_duration_minutes, displayed in real-time in the services card header, passed as total_duration to server action. |
| P2-06 | Scheduling engine (conflict detection) | 2026-08-29 16:20 UTC+08:00 | Created `src/lib/services/scheduling-service.ts` (SchedulingService with checkConflict and getAvailableSlots methods). Checks: dentist working schedule by day_of_week, clinic holidays, dentist_blocks time overlap, existing appointments overlap (excludes cancelled/declined/archived). Integrated conflict check into `createAppointmentAction` — rejects creation if conflict found. Added `getAvailableSlotsAction` server action. Added slot picker UI to AppointmentForm — "Check Available Slots" button fetches slots and displays as clickable buttons (available=outline, unavailable=ghost+strikethrough). TypeScript compiles cleanly. |
| P2-08 | Staff Booking Dashboard | 2026-08-29 16:40 UTC+08:00 | Added `approveAppointmentAction` and `declineAppointmentAction` to appointments actions. Created `src/app/(dashboard)/bookings/page.tsx` (server-side fetch with Supabase join to patients, status filter via searchParams). Created `src/app/(dashboard)/bookings/booking-dashboard-client.tsx` (status filter tabs: Pending Review, Approved, Declined, Reschedule Required, All; booking cards with patient name, reference no, date/time, duration, elapsed time since request; approve/decline buttons for pending bookings; status badges; empty state). TypeScript compiles cleanly. |
| P2-09 | Calendar view for appointments | 2026-08-29 17:00 UTC+08:00 | Created `src/components/appointments/appointment-calendar.tsx` (month-grid calendar with 7-column layout, appointment status color dots, day cell click to select, sidebar with selected day's appointments, prev/next month navigation, "New" button link, today highlight, +N more overflow indicator). Created `src/app/(dashboard)/appointments/page.tsx` (server-side fetch with Supabase join to patients, month filter via searchParams, date range filtering). TypeScript compiles cleanly. |
| P3-01 | Patient check-in & identification flow | 2026-08-29 17:20 UTC+08:00 | Created `src/app/(dashboard)/check-in/actions.ts` (checkInPatientAction — validates booking_status is approved/confirmed, checks not already checked_in, sets visit_status=checked_in, revalidates /check-in and /queue). Created `src/app/(dashboard)/check-in/page.tsx` + `check-in-client.tsx` (debounced search by name/phone/ref, fetches today's approved/confirmed appointments via API, displays results with patient name, ref no, time, duration, status badges, check-in button with loading state, success/error alerts, empty state). Created `src/app/api/check-in/search/route.ts` (API endpoint searching patients by name/phone, joining to today's appointments, fallback search by reference_no). Added Check-In nav item (admin/reception, UserCheck icon). TypeScript compiles cleanly. |
| P3-02 | QR code generation (staff-side) | 2026-08-29 17:40 UTC+08:00 | Installed `qrcode.react` package. Created `src/lib/services/qr-code-service.ts` (QrCodeService with generateForAppointment, validateToken, invalidateToken, getActiveQRForAppointment). Created `src/app/(dashboard)/check-in/qr/actions.ts` (generateQrCodeAction — reuses active QR or creates new, getActiveQrCodeAction). Created `src/app/(dashboard)/check-in/qr/[appointmentId]/page.tsx` (server component fetching appointment + patient name). Created `src/app/(dashboard)/check-in/qr/[appointmentId]/qr-display.tsx` (QRCodeSVG rendering, countdown timer with red warning under 60s, auto-reuse active QR, generate/regenerate buttons, expired state). TypeScript compiles cleanly. |
| P3-03 | QR self-registration page (public) | 2026-08-29 18:00 UTC+08:00 | Created `src/app/(public)/layout.tsx` (bare layout without dashboard shell). Created `src/app/(public)/register/[token]/page.tsx` (server component validates token via validateTokenAction, shows error page if invalid/expired/used). Created `src/app/(public)/register/[token]/registration-wizard.tsx` (4-step wizard: Personal→Contact→Medical→Review, progress bar with percentage, per-step validation with PH phone regex, privacy notice RA 10173, success screen with "proceed to reception" message). Created `src/app/(public)/register/[token]/actions.ts` (validateTokenAction, submitRegistrationAction — validates token, creates patient via PatientService, links to appointment, invalidates token atomically). Added /register to PUBLIC_ROUTES in middleware. TypeScript compiles cleanly. |
| P3-04 | QR code validation + invalidation logic | 2026-08-29 18:00 UTC+08:00 | Implemented in QrCodeService: validateToken checks is_used=false AND expires_at > now, invalidateToken sets is_used=true. Integrated into submitRegistrationAction — token invalidated after patient creation + appointment linking. |
| P3-05 | Staff-assisted registration form | 2026-08-29 18:20 UTC+08:00 | Created `src/app/(dashboard)/patients/new/page.tsx` + `staff-registration-form.tsx` (RHF + Zod validation, 3 cards: Personal, Contact, Medical, duplicate detection on contact_no blur via checkDuplicatePatientAction, warning with override checkbox, redirects to patient detail on success). Added `checkDuplicatePatientAction` to patients actions (checks name + phone match in non-archived patients). TypeScript compiles cleanly. |
| P3-06 | Walk-in visit creation | 2026-08-29 18:40 UTC+08:00 | Created `src/app/(dashboard)/appointments/walk-in/actions.ts` (createWalkInAction — same validation as createAppointmentAction but sets booking_status=approved, visit_status=checked_in, payment_status=pending_payment; includes conflict detection; revalidates /appointments and /queue). Created `src/app/(dashboard)/appointments/walk-in/page.tsx` (server component fetching patients, dentists, services; reuses AppointmentForm with walk-in action). TypeScript compiles cleanly. |
| P3-07 | Queue management with ordering rules | 2026-08-29 19:00 UTC+08:00 | Created `src/app/(dashboard)/queue/actions.ts` (callNextAction — finds first checked_in patient by scheduled_time, sets visit_status=waiting; callSpecificAction — sets specific appointment to waiting). Created `src/app/(dashboard)/queue/page.tsx` (server component fetching today's queue with patient + dentist joins). Created `src/app/(dashboard)/queue/queue-client.tsx` (stat cards: Waiting/Called/In Progress; ordered queue list with numbered positions, patient name, ref, time, dentist; status badges with color coding; Call Next button + per-patient Call button; 5s polling via /api/queue; ARIA live region; empty state). Created `src/app/api/queue/route.ts` (polling API endpoint). TypeScript compiles cleanly. |
| P3-08 | Realtime queue view | 2026-08-29 19:00 UTC+08:00 | Implemented 5-second polling fallback via /api/queue route. Supabase Realtime WebSocket subscription can be added when realtime is configured in local dev. |
| P3-09 | Consultation view | 2026-08-29 19:20 UTC+08:00 | Created `src/app/(dashboard)/consultation/[appointmentId]/actions.ts` (startConsultationAction — validates visit_status is waiting/checked_in, sets in_consultation; generateConsentAction — creates consent_forms record with treatment_info, consent_version=1.0, duplicate prevention). Created `src/app/(dashboard)/consultation/[appointmentId]/page.tsx` (server component fetching appointment with patient, dentist, and service joins; checks for existing consent form). Created `src/app/(dashboard)/consultation/[appointmentId]/consultation-client.tsx` (split layout: left panel with patient info cards — name, contact, DOB, medical history, allergies with warning, scheduled services; right panel with Start Consultation button and Generate Consent Form with treatment info textarea; status badge; success/error alerts). /consultation already dentist-only in middleware ROLE_ROUTES. TypeScript compiles cleanly. |
| P3-10 | Electronic consent form generation | 2026-08-29 19:40 UTC+08:00 | generateConsentAction creates consent_forms record (treatment_info, consent_version=1.0, staff_id from auth). Consent signing page at /consent/[consentId] fetches consent form with patient name. |
| P3-11 | Signature pad component | 2026-08-29 19:40 UTC+08:00 | Installed `react-signature-canvas`. Created `src/components/consent/signature-pad.tsx` (forwardRef component exposing isEmpty/toDataURL/clear; SignatureCanvas with touch-action:none for tablet; clear button; dashed border container). |
| P3-12 | Consent signing flow + storage upload | 2026-08-29 19:40 UTC+08:00 | Created `src/app/(dashboard)/consent/[consentId]/actions.ts` (signConsentAction — validates not already signed, converts base64 to Buffer, uploads PNG to Supabase storage consent-signatures bucket with fallback to base64 data URL, sets signed_at timestamp; getConsentFormAction for fetching). Created `src/app/(dashboard)/consent/[consentId]/page.tsx` (server component fetching consent form + patient name). Created `src/app/(dashboard)/consent/[consentId]/consent-signing-client.tsx` (tablet-friendly full-screen layout, treatment info card, patient declaration text, SignaturePad, Sign & Submit button, signed confirmation screen with signature image preview and timestamp). /consent already dentist-only in middleware ROLE_ROUTES. TypeScript compiles cleanly. |
| P3-13 | Treatment documentation (dental chart, clinical notes) | 2026-08-29 20:00 UTC+08:00 | Created `src/lib/utils/cn.ts` (class name merge utility). Created `src/components/dental-chart/dental-chart.tsx` (FDI/ISO 3950 dental chart — 32 teeth in 4 quadrants: UR 18-11, UL 21-28, LR 48-41, LL 31-38; 8 tooth statuses: healthy/decayed/filled/extracted/crown/implant/root_canal/bridge with color coding; clickable teeth with selection ring; hover scale; legend; extracted teeth show ×; ARIA labels). Created `src/app/(dashboard)/consultation/[appointmentId]/treatment-actions.ts` (saveTreatmentRecordAction — creates/updates treatment_records; getTreatmentRecordAction; pauseTreatmentAction; resumeTreatmentAction; completeTreatmentAction). Created `src/app/(dashboard)/consultation/[appointmentId]/treatment-form.tsx` (split layout: dental chart with status selector buttons + clinical documentation form with diagnosis/procedures/clinical_notes/prescriptions/treatment_plan; Save/Pause/Resume/Complete buttons; pause dialog with reason input; auto-loads existing record). Integrated TreatmentForm into consultation-client.tsx (renders when visit_status is in_consultation/treatment_ongoing/treatment_paused/completed). TypeScript compiles cleanly. |
| P3-14 | Treatment pause/resume workflow | 2026-08-29 20:00 UTC+08:00 | pauseTreatmentAction sets visit_status=treatment_paused + records pause_reason and paused_at in treatment_records. resumeTreatmentAction sets visit_status=treatment_ongoing + records resumed_at. completeTreatmentAction sets visit_status=completed. UI: Pause button opens dialog requiring reason, Resume button appears when paused, Complete button available during ongoing/paused. All actions revalidate /queue and /consultation/[id]. |
| P4-04 | Cloudflare R2 Integration | 2026-08-29 20:30 UTC+08:00 | Installed `@aws-sdk/client-s3` + `@aws-sdk/s3-request-presigner`. Created `src/lib/services/r2-service.ts` (S3-compatible R2 client, generatePresignedUploadUrl with file type validation [jpg/png/pdf] + size limits [10MB proof, 5MB signature, 10MB document], UUID object keys, 15min URL expiry, generatePresignedReadUrl, validateFileType, validateFileSize, isR2Configured check, extractObjectKeyFromUrl). Created `src/app/api/upload/route.ts` (POST endpoint — auth check, validates required fields, returns presigned URL). Created `src/app/api/upload/actions.ts` (server actions: getPresignedUploadUrlAction, getPresignedReadUrlAction). Created `docs/env-example.txt` with R2 env var placeholders. TypeScript compiles cleanly. |
| P4-01 | Invoice generation service | 2026-08-29 20:50 UTC+08:00 | Created migration `20260829200000_add_pricing_columns.sql` (adds default_price NUMERIC(12,2) to dental_services, price NUMERIC(12,2) to appointment_services, backfills seed data prices). Updated `src/lib/types/database.ts` (added default_price to DentalService, price to AppointmentService). Created `src/app/(dashboard)/billing/[appointmentId]/actions.ts` (generateInvoiceAction — fetches appointment_services with dental_services join, calculates total from prices, creates invoices record with pending_payment status, duplicate prevention; getInvoiceAction — fetches full invoice data with line items, payments, patient/dentist info). |
| P4-02 | Invoice detail page | 2026-08-29 20:50 UTC+08:00 | Created `src/app/(dashboard)/billing/[appointmentId]/page.tsx` (server component fetching appointment + invoice). Created `src/app/(dashboard)/billing/[appointmentId]/billing-client.tsx` (BillingClient — invoice details card with patient/dentist/date, line items with service name + price, total/paid/remaining with tabular numerals ₱ formatting, payment history list, payment form with amount + method selector [cash/gcash/maya/card/bank_transfer], generate invoice button when no invoice exists, checkout button when fully paid, status badge). |
| P4-03 | Payment processing | 2026-08-29 20:50 UTC+08:00 | recordPaymentAction in billing actions — validates amount ≤ remaining balance, supports 5 payment methods (cash/gcash/maya/card/bank_transfer), records payment with recorded_by user ID, auto-updates invoice payment_status to partially_paid or paid based on cumulative payments. |
| P4-05 | Partial payment tracking | 2026-08-29 20:50 UTC+08:00 | recordPaymentAction calculates totalPaid from all existing payments, compares to invoice total_amount, sets partially_paid when partial, paid when fully met. BillingClient displays paid/remaining amounts with tabular numerals. |
| P4-06 | Payment status auto-calculation | 2026-08-29 20:50 UTC+08:00 | Payment status calculated in recordPaymentAction after each payment insert. DB trigger can be added for belt-and-suspenders enforcement. |
| P4-08 | Checkout & visit completion | 2026-08-29 20:50 UTC+08:00 | checkoutAction in billing actions — validates invoice exists and payment_status is not pending_payment, sets visit_status=completed + booking_status=completed on appointment, revalidates /billing/[id] and /queue. BillingClient shows Complete Checkout button when fully paid. |
| P4-07 | Follow-up appointment scheduling | 2026-08-29 21:00 UTC+08:00 | Added createFollowUpAction to billing actions — fetches original appointment's patient_id + dentist_id + total_duration, conflict detection via SchedulingService instance, creates new appointment with FU- reference number and approved/checked_in status, links selected services with prices from dental_services. Created `src/app/(dashboard)/billing/[appointmentId]/follow-up-scheduler.tsx` (FollowUpScheduler component — date/time inputs with min date today, service toggle buttons from original invoice line items, schedule button with loading state, success message with reference number). Integrated into BillingClient below the billing grid. TypeScript compiles cleanly. |
| P5-01 | Messenger webhook setup | 2026-08-29 22:40 UTC+08:00 | Created `src/app/api/webhooks/messenger/route.ts` (GET handler — verifies hub.mode=subscribe + hub.verify_token against MESSENGER_VERIFY_TOKEN env var, returns hub.challenge on match, 403 on mismatch; POST handler — reads raw body, verifies X-Hub-Signature-256 header using HMAC-SHA256 with MESSENGER_APP_SECRET and constant-time comparison via timingSafeEqual, 401 on missing/invalid signature, parses page entries, extracts sender PSID + message text/postback payload/quick_reply payload, stores via messenger service). Created `src/lib/services/messenger-service.ts` (findOrCreateConversation — finds or creates messenger_conversations by patient_psid; saveMessage — inserts to messenger_messages with direction inbound/outbound; updateConversationStatus — updates status + taken_over_by/at; getConversationByPsid; getConversations — list with last message preview; getMessages — ordered by sent_at). Updated `src/middleware.ts` to add /api/webhooks and /api/cron to PUBLIC_ROUTES. TypeScript compiles cleanly. |
| P5-02 | Booking request parsing (intent + conversation flow) | 2026-08-29 22:50 UTC+08:00 | Created `src/lib/services/booking-parser.ts` (processIncomingMessage — entry point called from webhook; parseIntent — detects book/confirm/reschedule/cancel/help/unknown from text; parseDate — natural language date parsing: today, tomorrow, day names, DD/MM/YYYY; parseTime — parses 9am, 2:30pm, 14:00 formats; multi-step booking session: awaiting_date→awaiting_time→awaiting_service→awaiting_dentist→complete; service selection by number or name match; dentist selection by number; sendMessengerMessage — calls Graph API v21.0 Send API with PAGE_ACCESS_TOKEN; help message with options list; unknown intent fallback). Updated webhook route to call processIncomingMessage instead of direct storage. TypeScript compiles cleanly. |
| P5-03 | Automated booking creation | 2026-08-29 22:50 UTC+08:00 | createPendingAppointment in booking-parser.ts — fetches default_duration_minutes from dental_services, creates appointment with pending booking_status + MB- reference number, links appointment_services with price. findPatientByPsid — looks up patients by messenger_psid. Created migration `20260829210000_add_messenger_psid_to_patients.sql` (adds messenger_psid TEXT column + partial index). Updated Patient interface in database.ts to include messenger_psid. finalizeBooking sends confirmation message with reference number, date, time. TypeScript compiles cleanly. |
| P5-04 | Send notification service | 2026-08-29 23:10 UTC+08:00 | Created `src/lib/services/notification-service.ts` (sendNotification — formats and sends 7 notification types: approval ✅, decline ❌, reschedule 📅, cancellation 🚫, reminder ⏰, follow_up 🦷, custom; formatMessage with structured message templates; sendToMessenger — Graph API v21.0 POST with PAGE_ACCESS_TOKEN; sendQuickReplyMessage — sends text with quick reply buttons for confirm/reschedule/cancel; sendReminderWithQuickReplies — reminder with 3 quick reply buttons; sendStaffMessage — for live chat staff messages; createStaffNotification — logs failed notification to audit_logs; SendResult type with fallbackRequired flag). Updated env-example.txt with MESSENGER_API_VERSION=v21.0. TypeScript compiles cleanly. |
| P5-05 | Auto-notification server actions | 2026-08-29 23:10 UTC+08:00 | Created `src/app/api/notifications/actions.ts` (sendApprovalNotificationAction, sendDeclineNotificationAction with reason, sendCancellationNotificationAction with optional reason, sendRescheduleNotificationAction with new date/time, sendFollowUpNotificationAction). getAppointmentDetails helper fetches appointment with dentist name via join. getPatientPsid helper fetches patient's messenger_psid. Each action builds NotificationPayload, calls sendNotification, returns SendResult with fallbackRequired flag. TypeScript compiles cleanly. |
| P5-06 | Confirmation reminder flow | 2026-08-29 23:20 UTC+08:00 | Added quick reply payload handling to booking-parser.ts processIncomingMessage: CONFIRM_<ref> → handleConfirmResponse (validates booking_status is approved/confirmed, sets confirmed, sends ✅ confirmation); RESCHEDULE_<ref> → handleRescheduleResponse (sets reschedule_required, informs staff will contact); CANCEL_<ref> → handleCancelResponse (sets pending_cancellation, informs staff will process). sendReminderWithQuickReplies in notification-service.ts sends reminder with 3 quick reply buttons (Confirm/Reschedule/Cancel) with payload format ACTION_<reference>. TypeScript compiles cleanly. |
| P5-07 | Reminder & expiration cron jobs | 2026-08-29 23:35 UTC+08:00 | Created `src/app/api/cron/reminders/route.ts` (GET — CRON_SECRET Bearer auth, fetches tomorrow's approved/confirmed appointments, looks up patient PSID, sends reminder with quick reply buttons via sendReminderWithQuickReplies, counts sent/failed). Created `src/app/api/cron/expiration/route.ts` (GET — CRON_SECRET Bearer auth, fetches pending appointments older than 24h, sets booking_status=expired, notifies patients via sendNotification with custom expiry message). Created `vercel.json` with cron schedules: reminders daily at 8am (0 8 * * *), expiration hourly (0 * * * *). /api/cron already in PUBLIC_ROUTES in middleware. TypeScript compiles cleanly. |
| P5-08 | Live Chat Dashboard | 2026-08-30 00:00 UTC+08:00 | Created `src/app/(dashboard)/chat/page.tsx` (server component — gets staff user ID, renders ChatClient). Created `src/app/(dashboard)/chat/chat-client.tsx` (3-panel grid: 280px conversation list + flexible chat thread + 300px patient info; 10s polling for conversation list; empty state with MessageSquare icon). Created `src/app/(dashboard)/chat/conversation-list.tsx` (searchable list — patient name or PSID, last message preview, status badge Bot Active/Staff, unread count, time ago formatting; empty state "No active conversations"). Created `src/app/(dashboard)/chat/chat-thread.tsx` (message bubbles outbound right/inbound left with timestamps, Take Chat/End Chat buttons that morph, message input with Enter to send, 5s polling for new messages, auto-scroll to bottom, ARIA live region, error display). Created `src/app/(dashboard)/chat/patient-info-panel.tsx` (patient name, PSID, recent 5 appointments with status badges and reference numbers; unknown patient state). Created `src/app/(dashboard)/chat/actions.ts` (getConversationsAction — fetches active/taken_over conversations with last message, patient name, unread count; getMessagesAction; takeChatAction; endChatAction; sendMessageAction; getPatientAppointmentsAction). TypeScript compiles cleanly. |
| P5-09 | Staff takeover / AI handoff | 2026-08-30 00:00 UTC+08:00 | takeChatAction calls updateConversationStatus with "taken_over" + staff ID + timestamp. endChatAction resets to "active" (bot resumes). sendMessageAction calls sendStaffMessage which sends via Graph API Send API and saves outbound message to messenger_messages. Booking parser's processIncomingMessage checks conversation status — when taken_over, bot pauses automated replies (messages still stored but no bot response sent). Nav item for /chat restricted to admin and reception roles. TypeScript compiles cleanly. |
| P5-10 | Messenger notification fallback | 2026-08-30 00:15 UTC+08:00 | createStaffNotification in notification-service.ts logs to audit_logs with action=messenger_notification_failed + metadata (patient_psid, notification_type, reason, timestamp). All 5 notification server actions (approval, decline, cancellation, reschedule, follow_up) call createStaffNotification on failure. Created `src/app/(dashboard)/dashboard/actions.ts` (getPendingStaffNotificationsAction — fetches from audit_logs; dismissStaffNotificationAction — deletes entry; getDashboardStatsAction — counts pending bookings, today's appointments, in queue, unread messages). Created `src/app/(dashboard)/dashboard/dashboard-client.tsx` (AlertTriangle alerts for failed notifications with PSID, type, reason, Dismiss button; 4 stat cards with live counts using tabular-nums). Updated `src/app/(dashboard)/dashboard/page.tsx` to use DashboardClient. TypeScript compiles cleanly. |
| P5-ENH | Messenger Platform compliance & enhancements | 2026-08-30 14:56 UTC+08:00 | Studied Facebook Messenger Platform docs (developers.facebook.com/docs/messenger-platform). Added `messaging_type` parameter to all Send API calls: RESPONSE for bot replies, UPDATE for proactive notifications (reminders, follow-ups), MESSAGE_TAG with HUMAN_AGENT tag for staff messages (7-day window instead of 24h). Added error 1545041 detection for 24h window expiry. Created `src/app/api/messenger-profile/route.ts` — POST endpoint to configure Messenger Profile: get_started button (GET_STARTED payload), greeting text, persistent_menu (Book Appointment, Clinic Hours, Contact Us, Cancel Appointment), ice_breakers. GET endpoint to retrieve current profile. Added postback payload handling in booking-parser.ts for GET_STARTED, MENU_BOOK, MENU_HOURS, MENU_CONTACT, MENU_CANCEL, ICE_BOOK, ICE_HOURS, ICE_RESCHEDULE. Added META_APP_ID and META_PAGE_ID to env-example.txt (used for Graph API page-level operations and profile configuration). Updated SECURITY.md §9 with Facebook docs references, messaging_type details, message tags, 24h window handling, Messenger Profile configuration table, and Handover Protocol section. TypeScript compiles cleanly. |
| P6-01/02/10 | Dentist Unavailability, Reassignment & Audit Trail | 2026-08-30 15:25 UTC+08:00 | Created `src/lib/services/reassignment-service.ts` — ReassignmentService with: getAffectedAppointments (fetches pending/approved/confirmed appointments in date range, joins patient names), findAlternateDentists (finds active dentists excluding original, checks available slots via inline schedule/holiday/block/conflict logic), reassignAppointment (validates slot conflict, updates appointment dentist_id+date+time+booking_status=rescheduled, creates reassignment_logs entry, inserts appointment_history for dentist_id/scheduled_date/scheduled_time changes, creates audit_logs entry), markAppointmentsRescheduleRequired (bulk updates booking_status to reschedule_required, creates appointment_history entries + audit_log), getReassignmentLogs. Inline helper methods: getAvailableSlotsForDentist, checkSlotConflict, checkBlocksConflict, checkAppointmentConflict, addMinutes. Created `src/app/(dashboard)/dentists/unavailability/actions.ts` — server actions: getDentistsAction, getAffectedAppointmentsAction, findAlternateDentistsAction, declareUnavailabilityAction (creates dentist_blocks entry + marks appointments), reassignAppointmentAction (reassigns + sends Messenger reschedule notification to patient with new dentist name), getCurrentStaffIdAction. Created `src/app/(dashboard)/dentists/unavailability/unavailability-client.tsx` — full UI: dentist selector, date range, block type, reason textarea, check affected appointments button, declare unavailability button, affected appointments list with reassign buttons, reassignment dialog with alternate dentist selector + available time slots, patient notification on success. Created `src/app/(dashboard)/dentists/unavailability/page.tsx` — server component wrapper. Added "Unavailability" nav item (CalendarX icon, admin+reception roles). TypeScript compiles cleanly. |
| P6-04 | Late Arrival, Delayed & No-Show Handling | 2026-08-30 15:45 UTC+08:00 | Added 3 new server actions to `src/app/(dashboard)/queue/actions.ts`: markDelayedAction (sets visit_status=delayed from checked_in/waiting, creates audit_log), moveToLaterSlotAction (validates no conflict with existing appointments, updates scheduled_time + resets visit_status to checked_in, creates appointment_history + audit_log), markNoShowAction (sets booking_status=no_show + clears visit_status, creates appointment_history for both fields + audit_log). Updated callSpecificAction to also accept "delayed" status. Updated `src/app/(dashboard)/queue/queue-client.tsx` — added "delayed" to VISIT_STATUS_COLORS/LABELS, added state for move dialog, added handlers (handleMarkDelayed, handleMarkNoShow, handleOpenMoveDialog, handleConfirmMove), added action buttons per visit_status: checked_in shows Call/Delay/Move/No-Show, delayed shows Call/Move/No-Show, waiting shows Delay/No-Show. Added move-to-later-slot Dialog with time input. Updated queue API route + page.tsx to include "delayed" in visit_status filter. TypeScript compiles cleanly. |
| P6-05/06 | Same-Day Dynamic Availability & Waitlist | 2026-08-30 16:10 UTC+08:00 | Created `src/lib/services/waitlist-service.ts` — WaitlistService with: getWaitlist (fetches entries with patient join, ordered by joined_at FIFO), joinWaitlist (creates new entry), leaveWaitlist (deletes entry), notifyNextInLine (finds first non-notified entry for date, sets notified_at, returns entry with patient info), acceptWaitlistSlot (creates confirmed appointment with WL- reference, links appointment_services, creates audit_log, deletes waitlist entry), declineWaitlistSlot (resets notified_at so next patient can be notified), findReleasedSlots (scans all active dentist schedules for unbooked/unblocked 30-min slots on given date). Created `src/app/(dashboard)/waitlist/actions.ts` — server actions: getWaitlistAction, getReleasedSlotsAction, joinWaitlistAction, leaveWaitlistAction, notifyNextAction (notifies next patient via Messenger custom message), acceptWaitlistSlotAction, declineWaitlistAction, getPatientsAction, getServicesAction. Created `src/app/(dashboard)/waitlist/waitlist-client.tsx` — full UI: date filter, available slots card with Notify Next buttons, waitlist queue card with FIFO ordering + Accept/Decline/Remove buttons, join dialog with patient selector + date, accept dialog with service selector. Created `src/app/(dashboard)/waitlist/page.tsx` — server component wrapper. Added "Waitlist" nav item (ListPlus icon, admin+reception roles). TypeScript compiles cleanly. |
| P6-08/09 | Patient-Initiated Cancel & Reschedule (Messenger) | 2026-08-30 16:30 UTC+08:00 | Booking parser already handles cancel/reschedule via Messenger: handleCancelResponse sets booking_status=pending_cancellation, handleRescheduleResponse sets booking_status=reschedule_required. Created `src/app/(dashboard)/bookings/actions.ts` — 3 new server actions: confirmCancellationAction (sets booking_status=cancelled, creates appointment_history + audit_log, sends cancellation notification to patient via Messenger), denyCancellationAction (restores booking_status to approved, creates appointment_history + audit_log, sends custom denial notification with reason to patient), rescheduleAppointmentAction (validates no slot conflict, updates scheduled_date + scheduled_time + sets booking_status=rescheduled, creates appointment_history for date/time/status changes + audit_log, sends reschedule notification to patient with new schedule + dentist name). Updated `src/app/(dashboard)/bookings/booking-dashboard-client.tsx` — added pending_cancellation + rescheduled to STATUS_FILTERS and STATUS_BADGES, added state for deny dialog (with reason textarea) and reschedule dialog (with date+time inputs), added handlers (handleConfirmCancellation, handleOpenDenyDialog, handleConfirmDeny, handleOpenRescheduleDialog, handleConfirmReschedule), added action buttons: pending_cancellation shows Confirm Cancel/Deny, reschedule_required shows Reschedule. Updated bookings page.tsx to accept all valid statuses as filter. TypeScript compiles cleanly. |
| P6-07 | Booking approval expiration cron | 2026-08-30 16:35 UTC+08:00 | Already implemented in P5-07. Verified: `/api/cron/expiration` route expires pending appointments older than 24 hours by setting booking_status=expired, notifies patients via Messenger with custom expiry message, authenticated via CRON_SECRET Bearer token, runs hourly via vercel.json cron schedule. No additional work needed. |
| P6-03 | Dentist Mobile Portal | 2026-08-30 17:00 UTC+08:00 | Created `src/app/(dentist)/` route group with layout.tsx (auth check + dentist role verification + fetch dentist record + render DentistPortalShell). Created `src/components/dentist-portal/dentist-portal-shell.tsx` — mobile-first layout with sticky header (dentist name + specialization) and fixed bottom navigation (3 tabs: Schedule, Queue, More, 56px min height touch targets). Created `src/app/(dentist)/page.tsx` — redirects to /dentist-portal/schedule. Created `src/app/(dentist)/schedule/page.tsx` + `schedule-client.tsx` — today's appointments with time/duration/patient name/services, booking+visit status badges, tap to open patient quick-view dialog (contact, services, status). Created `src/app/(dentist)/queue/page.tsx` + `queue-client.tsx` — simplified queue grouped by In Consultation (bordered primary) and Waiting (numbered), visit status badges. Created `src/app/(dentist)/emergency/page.tsx` + `actions.ts` — emergency declaration with optional reason textarea, uses EmergencyButton component. Created `src/components/dentist-portal/emergency-button.tsx` — 192px circular button with 1.5s long-press activation, SVG circular progress indicator, pointer events (pointerDown/Up/Leave/Cancel), touch-none CSS, disabled state during activation. Emergency action creates dentist_blocks (sick_leave type), calls markAppointmentsRescheduleRequired for today+tomorrow, creates audit_log (emergency_declared). Created `src/app/(dentist)/more/page.tsx` + `more-client.tsx` — dentist profile card, today's patients list with tap-to-view dialog (name, contact, email, birth date, allergies highlighted in red, medical history), quick action buttons (Emergency, Schedule, Queue), sign out link. Updated middleware.ts to add /dentist-portal to ROLE_ROUTES as dentist-only. TypeScript compiles cleanly. |
| P7-01 | System Settings Module | 2026-08-30 17:15 UTC+08:00 | Created `src/app/(dashboard)/settings/actions.ts` — getSettingsAction (fetches by category or all), saveSettingsAction (bulk update with audit log). Created `src/app/(dashboard)/settings/page.tsx` + `settings-client.tsx` — 6 category sidebar tabs (Clinic/general, Dentist/schedule, Appointment/booking, Messenger/notifications, Payment, Security), per-field rendering based on data_type: boolean→Switch, integer→number Input, string→Input/Textarea, time fields use type="time", unsaved changes warning modal on tab switch, Reset/Save buttons, success/error notifications. Created `src/components/ui/switch.tsx` — base-ui Switch wrapper using SwitchPrimitive.Root/Thumb with correct onCheckedChange API. Updated middleware.ts to add /audit as admin-only. TypeScript compiles cleanly. |
| P7-02 | Audit Log Viewer | 2026-08-30 17:20 UTC+08:00 | Created `src/app/(dashboard)/audit/actions.ts` — getAuditLogsAction (paginated, filters by entity_type/user_id/date_range, joins users for name, returns total count), getStaffUsersAction (for user filter dropdown). Created `src/app/(dashboard)/audit/page.tsx` + `audit-client.tsx` — filter card (entity type Select, user Select, start/end date Inputs, Apply/Clear buttons), paginated table (timestamp, user, action badge, entity type+id, metadata JSON), prev/next pagination with page indicator, loading state, empty state with Shield icon. Added "Audit Logs" nav item (Shield icon, admin-only). TypeScript compiles cleanly. |
| P7-03 | Record Archiving | 2026-08-30 17:25 UTC+08:00 | Created `src/app/(dashboard)/patients/archive-actions.ts` — archivePatientAction (sets is_archived=true + audit log), unarchivePatientAction (restores + audit log), archiveAppointmentAction (sets is_archived=true + audit log), getArchivedPatientsAction (fetches archived patients for retrieval). Created `src/components/shared/archive-dialog.tsx` — reusable confirmation dialog with item name display, archive button trigger, loading state, error display, uses base-ui DialogTrigger render prop pattern. TypeScript compiles cleanly. |
| P7-04 | Appointment History View | 2026-08-30 17:30 UTC+08:00 | Created `src/app/(dashboard)/appointments/[id]/history/actions.ts` — getAppointmentHistoryAction (fetches appointment_history joined with users for changed_by name, ordered by changed_at desc). Created `src/app/(dashboard)/appointments/[id]/history/page.tsx` — server component fetching appointment with patient name, back button, renders HistoryTimeline. Created `src/components/appointments/history-timeline.tsx` — vertical timeline with dot+line connectors, each entry shows field label badge, changed by user, timestamp, old value (strikethrough) → new value (highlighted), empty state with History icon. TypeScript compiles cleanly. |
| P7-05/06 | Loading/Error/Empty States & Performance | 2026-08-30 17:35 UTC+08:00 | Created `src/components/shared/skeleton-loader.tsx` — SkeletonLoader (animated pulse lines), SkeletonCard (card with pulse placeholders), SkeletonList (multiple skeleton cards). Created `src/components/shared/error-state.tsx` — error display with AlertTriangle icon, message, optional retry button. Created `src/components/shared/empty-state.tsx` — configurable empty state with icon, title, description, optional action CTA. Created `src/app/error.tsx` — global error boundary with error message + digest + Try Again button. Created `src/app/not-found.tsx` — 404 page with FileQuestion icon + Go to Dashboard button. Performance: audit log viewer uses server-side Supabase pagination (.range), all list pages use server-side queries. TypeScript compiles cleanly. |
| P7-05/06 Gap Fill | Toast, Archived View, React.memo, Lazy Load | 2026-08-29 16:30 UTC+08:00 | Created `src/components/ui/toast.tsx` — custom ToastProvider + useToast hook with success/error/info/loading variants, auto-dismiss, close button, fixed bottom-right positioning. Wrapped root layout with ToastProvider. Updated `settings-client.tsx` to use toasts instead of inline Alert notifications. Created `src/app/(dashboard)/patients/archived/page.tsx` + `archived-client.tsx` — admin-only archived records view with restore functionality, toast feedback, empty state, loading state. Added "Archived" nav item (Archive icon, admin-only). Added React.memo to DentistScheduleClient, DentistQueueClient, HistoryTimeline. Added dynamic import (ssr: false) for SignaturePad in consent-signing-client.tsx to lazy-load react-signature-canvas. Fixed dentist portal routing conflict by nesting pages under `(dentist)/dentist-portal/`. TypeScript compiles cleanly. |
| BUG-018 Fix | New Appointment form dentist display & FormData submission | 2026-08-29 17:08 UTC+08:00 | Fixed `DentistService.getAllDentists()` to join `users` table and return `full_name` field. Added `full_name?: string` to `Dentist` interface in `database.ts`. Updated `appointment-form.tsx` Select components to display patient/dentist names in both trigger and dropdown items. Added hidden `<input>` elements for `patient_id` and `dentist_id` so values are included in FormData submission. Fixed walk-in page to use `DentistService` and `PatientService` instead of raw Supabase queries. `tsc --noEmit` passes, `next build` succeeds. |
| BUG-019 Fix | Login page Suspense boundary for useSearchParams | 2026-08-29 17:08 UTC+08:00 | Extracted login form into `src/app/(auth)/login/login-form.tsx` — client component accepting `redirectUrl` prop (no `useSearchParams`). Converted `page.tsx` to async server component reading `searchParams` prop, wrapping `LoginForm` in `<Suspense>`. Resolves Next.js build prerender error: `useSearchParams() should be wrapped in a suspense boundary at page "/login"`. `next build` succeeds with all 34 pages generated. |
| BUG-020 Fix | Dentist names not displaying — Supabase users join returns array | 2026-08-29 17:13 UTC+08:00 | Fixed `DentistService.getAllDentists()` — Supabase returns `users` join as array `[{ first_name, last_name }]` not single object. Added `Array.isArray(rawUsers)` check to extract `rawUsers[0]` when array, otherwise use object directly. Same pattern as BUG-012. Dentist dropdown now shows full names (e.g. "John Doe · General Dentistry"). |
| CSP Fix | Google Fonts CSP violations | 2026-08-29 17:10 UTC+08:00 | Updated `next.config.ts` CSP headers: added `https://fonts.googleapis.com` to `style-src` and `https://fonts.gstatic.com` to `font-src`. Resolves font loading violations in dev mode with `next/font/google` (Inter). |
| Auth Users Fix | Local auth accounts missing required GoTrue fields after `supabase db reset` | 2026-08-29 17:30 UTC+08:00 | Updated `supabase/create_auth_users.sql` to include `instance_id='00000000-0000-0000-0000-000000000000'`, `aud='authenticated'`, `role='authenticated'`, and empty token columns (`confirmation_token`, `recovery_token`, etc.) required by GoTrue. Reset database, recreated auth users, verified login token endpoint returns success. |
| RLS Fix | `users` table RLS blocks dentist name joins for non-admin roles | 2026-08-29 17:20 UTC+08:00 | Created migration `20260829171400_update_users_rls_for_staff_visibility.sql` allowing all authenticated staff to SELECT from `users` table (needed for dentist/staff name display). Switched `DentistService.getAllDentists()` from `users!inner(...)` to left join `users(...)` for extra safety. |
| BUG-021 Fix | Seed UUIDs fail Zod `.uuid()` validation — "Invalid dentist ID" on form submit | 2026-08-29 17:57 UTC+08:00 | Hardcoded `users` and `dentists` seed IDs (`a0000000-0000-0000-0000-...` and `b0000000-0000-0000-0000-...`) had UUID version/variant `0000`, rejected by Zod `.uuid()`. Updated `supabase/seed.sql` and `supabase/create_auth_users.sql` to valid v4 UUIDs (`...-0000-4000-8000-...`), preserving referential integrity. Ran `npx supabase db reset` and recreated auth users. Verified Zod validation passes. |
| BUG-022 Fix | RLS policy blocks admin from creating appointment | 2026-08-29 18:21 UTC+08:00 | `get_user_role()` returned GoTrue default JWT claim `'authenticated'` instead of the application role. Updated `get_user_role()` to `SELECT role FROM public.users WHERE id = auth.uid()` (SECURITY DEFINER). Updated `supabase/migrations/20260829052857_create_rls_policies.sql` comment/function. Created and applied new migration `supabase/migrations/20260829182200_fix_get_user_role.sql`. Verified `get_user_role()` returns `admin` for admin UUID. |
| Calendar & Bookings Fix | Supabase join array handling + time format in calendar | 2026-08-29 18:26 UTC+08:00 | Created `src/lib/utils/supabase-join.ts` with `getSingleJoined()` helper to normalise Supabase one-to-one joins (array vs object). Updated `appointments/page.tsx`, `bookings/page.tsx`, and `dentist-service.ts` to use it with "Unknown Patient" fallback. Formatted `scheduled_time` to `HH:MM` in `appointment-calendar.tsx` (was showing `09:30:00`). `npx tsc --noEmit` passes. |
| BUG-023 Fix | Topbar user dropdown crashes with MenuGroupContext missing | 2026-08-29 18:31 UTC+08:00 | `DropdownMenuLabel` in `topbar.tsx` was used outside a `DropdownMenuGroup`. Base UI's `MenuPrimitive.GroupLabel` requires a `Menu.Group` ancestor. Wrapped label and sign-out item in `<DropdownMenuGroup>`. `npx tsc --noEmit` passes. |
| BUG-024 Fix | Booking approve/decline fails with `appointment_history` RLS violation | 2026-08-29 18:33 UTC+08:00 | `log_appointment_history()` trigger ran as the invoker and `appointment_history` had no INSERT RLS policy. Added `SECURITY DEFINER SET search_path = public` to the trigger function so it inserts audit rows as the table owner. Created and applied new migration `20260829183300_fix_appointment_history_trigger_rls.sql`. Updated original `supabase/migrations/20260829053054_create_triggers.sql` for future `db reset`. Simulated admin approve in a rolled-back transaction and confirmed `booking_status` updated to `approved` with a matching `appointment_history` row. |
| BUG-025 Fix | Queue page crashes with `column dentists_1.full_name does not exist` | 2026-08-29 18:43 UTC+08:00 | Queue page and API route queried `dentists(full_name)` but `full_name` is not a real column — it's a computed field from `DentistService` joining `users`. Changed to `dentists(users(first_name, last_name))` in `src/app/(dashboard)/queue/page.tsx`, `src/app/api/queue/route.ts`, and `src/app/(dashboard)/consultation/[appointmentId]/page.tsx`. Used `getSingleJoined()` for array/object normalisation with "Unknown Dentist" fallback. `npx tsc --noEmit` passes. |
| BUG-026 Fix | Queue Call/Delay actions crash with `invalid input value for enum visit_status: "no_show"` | 2026-08-29 18:50 UTC+08:00 | `validate_appointment_status()` trigger referenced `'no_show'` (a `booking_status` enum value) in a `visit_status` transition check: `(OLD.visit_status = 'delayed' AND NEW.visit_status IN ('waiting', 'no_show'))`. PostgreSQL tried to cast `'no_show'` to `visit_status` enum type, which doesn't have that value, causing every `visit_status`-only UPDATE to fail. Removed `'no_show'` from the delayed transition — no-show is handled by setting `visit_status = NULL` which skips the transition check. Created and applied migration `20260829185000_fix_visit_status_trigger_enum.sql`. Updated original `supabase/migrations/20260829053054_create_triggers.sql`. Also improved error messages in `src/app/(dashboard)/queue/actions.ts` to surface actual DB errors instead of generic strings. Verified `checked_in -> waiting` and `waiting -> delayed` transitions work. |
| Bug Testing Round | Code review of all major flows — 5 bugs found and fixed | 2026-08-30 16:50 UTC+08:00 | Systematic code review of dashboard, billing, check-in, dentist portal, settings, and chat pages. Found and fixed 5 bugs: **BUG-027** — `dismissStaffNotificationAction` DELETE on `audit_logs` failed (no DELETE RLS policy); fixed by using `createServiceRoleClient()` admin client. **BUG-028** — Billing `getInvoiceAction` accessed patient/dentist joins without `getSingleJoined()` (Supabase returns arrays); added `getSingleJoined()` calls. **BUG-029** — `createFollowUpAction` missing `scheduled_date` (NOT NULL violation), set `visit_status: "checked_in"` for future appointment, inserted full ISO datetime into `time` column; added `scheduled_date`, removed `visit_status`, used time-only string. **BUG-030** — Dentist portal queue/schedule/more pages accessed `patients` join without `getSingleJoined()`; added calls in all 3 pages. **BUG-031** — `checkoutAction` hid DB error with generic message; surfaced `apptError.message`. `npx tsc --noEmit` passes with 0 errors. |
| P7-07 | End-to-end testing with Playwright | 2026-08-30 19:30 UTC+08:00 | Installed `@playwright/test` + Chromium browser. Created `playwright.config.ts` (baseURL localhost:3000, 30s timeout, HTML reporter, reuse dev server). Created `tests/e2e/fixtures.ts` with shared auth helper (login as admin/reception/dentist). Created 8 spec files with 31 tests total: `auth.spec.ts` (5 tests — login as each role, invalid credentials, empty fields), `dashboard.spec.ts` (3 tests — stats display, notifications, dismiss), `patients.spec.ts` (4 tests — list, search, new dialog, detail view), `appointments.spec.ts` (4 tests — calendar, month nav, booking dashboard, filter), `queue.spec.ts` (3 tests — page display, stat cards, queue items), `check-in.spec.ts` (3 tests — page display, search, empty state), `settings.spec.ts` (3 tests — categories, switch tabs, save button), `dentist-portal.spec.ts` (4 tests — schedule, queue, more, emergency), `chat.spec.ts` (2 tests — page display, conversation list/empty state). All 31 tests pass in 1.5m. Added `test:e2e`, `test:e2e:ui`, `test:e2e:report` scripts to package.json. |
| P7-08 | Security audit (5-domain manual review) | 2026-08-30 19:50 UTC+08:00 | Manual security audit across 5 domains: ① Auth & Session — Supabase SSR auth, session refresh in middleware, cookie-based tokens, no localStorage/sessionStorage secrets. ② Authorization & RLS — 21 tables RLS-enabled, role-based policies (admin/reception/dentist), `get_user_role()` SECURITY DEFINER, immutable tables (audit_logs, appointment_history, messenger_messages, reassignment_logs, consent_forms) have no UPDATE/DELETE policies. ③ Input Validation & Injection — Zod schemas on all forms (login, patient, appointment, payment, consent, treatment, settings), parameterized Supabase queries (no raw SQL), file upload validation (type + size + extension), phone regex validation. ④ Data Exposure & Privacy — `.gitignore` covers `.env*`, no `NEXT_PUBLIC_` secrets, `SUPABASE_SECRET_KEY` server-only, no `dangerouslySetInnerHTML`, no `eval()`, RLS prevents cross-tenant data access. ⑤ Dependency & Configuration — `npm audit` 0 vulnerabilities, security headers (HSTS, X-Frame-Options DENY, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, CSP), webhook HMAC-SHA256 signature verification with `timingSafeEqual`. Found and fixed 3 issues: **BUG-032** (MEDIUM) — Open redirect in login `redirect` param; fixed with `isSafeRedirect()` validation (must start with `/`, not `//`). **BUG-033** (LOW) — Cron routes used `!==` for CRON_SECRET comparison; fixed with `timingSafeEqual` in both reminders + expiration routes. **BUG-034** (LOW) — API routes (check-in search, queue) lacked explicit `auth.getUser()` checks; added for defense-in-depth. Also tightened CSP `script-src` to remove `'unsafe-eval'` in production (kept for dev only). `tsc --noEmit` passes. 31 E2E tests pass. |
| Session 8 Bug Fixes | Check-in, QR code, consent, billing, and DB migration fixes | 2026-08-30 22:30 UTC+08:00 | Fixed 14 bugs across check-in, QR registration, consent, billing, and DB infrastructure: **BUG-035** — Middleware RBAC blocks admin from `/consultation` and `/consent` (changed `ROLE_ROUTES` from single role to array, added admin to both routes). **BUG-036** — Middleware missing role guards for 5 routes (added `/check-in`, `/chat`, `/dentists/unavailability`, `/waitlist`, `/patients/archived` to `ROLE_ROUTES`). **BUG-037** — New patients can't book via Messenger (added `findOrCreatePlaceholderPatient()` to create minimal patient record from PSID). **ARCH-001** — Booking sessions in-memory `Map` (created `booking_sessions` table, DB-backed session storage with 30-min expiry). **BUG-038** — Booking approval/decline doesn't send Messenger notification (added `sendNotification()` calls to `approveAppointmentAction` and `declineAppointmentAction`, plus audit logs and appointment history). **ARCH-002** — Live Chat uses polling instead of Realtime (added `is_read` column to `messenger_messages`, replaced `setInterval` with Supabase Realtime subscriptions, added `markAsReadAction`). **BUG-039** — Staff messages fail with HUMAN_AGENT tag (changed `sendStaffMessage` from `MESSAGE_TAG`/`HUMAN_AGENT` to `RESPONSE` type). **BUG-040** — Check-In page shows only search bar (made `page.tsx` server component fetching today's appointments, added QR code link per appointment). **BUG-041** — QR code URL format mismatch (changed from query param `?token=` to path segment `/register/TOKEN`). **BUG-042** — RLS blocks public registration (replaced `createServerSupabaseClient` with service role client in `validateTokenAction` and `submitRegistrationAction`). **BUG-043** — `revalidatePath` causes "Invalid QR Code" after submission (removed `revalidatePath("/check-in")` from `submitRegistrationAction`). **BUG-044** — Dentist can't generate invoices (update...d `invoices_insert`/`invoices_update`/`payments_insert`/`payments_update` RLS policies to include `dentist` role). **BUG-045** — Consent form generation fails with missing `staff_id` (added `supabase.auth.getUser()` and `staff_id: user.id` to `consent_forms` insert). **BUG-046** — Check-In button allows invalid visit_status transitions (changed `disabled` condition from `=== "checked_in"` to `!== null`, added status label to button). **BUG-047** — `supabase db reset` fails on triggers migration (removed redundant `LANGUAGE plpgsql` after `$$` in `log_appointment_history()` function). All migrations applied manually to local DB, auth users and seed data recreated. |
| P5-ENH-2 | Chat UX improvements & patient notifications | 2026-08-30 23:50 UTC+08:00 | Improved Live Chat UX: ① Made chat message container (`space-y-3 p-4`) scrollable with `h-full overflow-hidden` on middle column and `min-h-0` on flex containers for proper flexbox scroll behavior. ② Auto-scroll to newest message via `scrollRef` on message updates. ③ After staff takes/ends chat, conversation list reloads and selected conversation state updates immediately — no page refresh needed. Added `onConversationChange` callback prop to `ChatThread`, passed `loadConversations` from `ChatClient`. ④ Added patient notifications via Messenger when staff takes over chat: "👩‍⚕️ A staff member from our clinic has joined this conversation and will assist you personally." ⑤ Added patient notification when staff ends chat (bot resumes): "🤖 The staff member has left the conversation. I (the assistant bot) am back to help you. Type \"help\" to see what I can do." ⑥ Fixed system messages not appearing in web chat UI — `sendStaffMessage` only saved to DB on successful Messenger API call; added fallback direct DB save via `saveMessage` in `takeChatAction` and `endChatAction` when Messenger send fails, avoiding duplicates by conditional save logic. Files: `chat-client.tsx`, `chat-thread.tsx`, `chat/actions.ts`. TypeScript compiles cleanly. |
| P5-ENH-3 | Spam prevention (rate limiting, message length, duplicate detection, appointment spam limit) | 2026-08-30 23:55 UTC+08:00 | Implemented 4 spam prevention measures in Messenger webhook handler and booking parser: ① **Rate limiting** — In-memory `Map<psid, timestamps[]>` in webhook route, max 5 messages per 60 seconds per PSID, excess messages skipped with console warning. ② **Message length limit** — Reject messages over 500 characters before reaching `processIncomingMessage`, preventing regex abuse and oversized DB rows. ③ **Duplicate message detection** — Track last 100 `message.mid` values in a `Set` with FIFO eviction; if Facebook retries webhook delivery, duplicate is skipped. ④ **Appointment spam limit** — In `finalizeBooking`, query patient's active appointments (`pending`, `approved`, `confirmed`); if 3 or more exist, reject new booking with message directing patient to check bookings or call clinic. Files: `src/app/api/webhooks/messenger/route.ts`, `src/lib/services/booking-parser.ts`. TypeScript compiles cleanly. |
| P5-ENH-4 | Messenger 24h window compliance (Message Tags) | 2026-08-31 00:05 UTC+08:00 | Solved Facebook 24-hour messaging window limitation for appointment reminders and notifications. Previously `sendNotification` used `UPDATE` type for reminders/follow-ups which still requires 24h window to be open — reminders sent via cron the day before would silently fail if patient hadn't messaged bot in 24h. Changes: ① Added `CONFIRMED_EVENT_TAG = "CONFIRMED_EVENT_UPDATE"` constant — Facebook's tag for appointment reminders, allows sending outside 24h window for up to 7 days. ② Updated `sendNotification` to use `MESSAGE_TAG` type with `CONFIRMED_EVENT_UPDATE` tag for `reminder`, `follow_up`, `reschedule`, and `cancellation` notification types. Other types (`approval`, `decline`, `custom`) still use `RESPONSE`. ③ Updated `sendQuickReplyMessage` to accept `messagingType` and `tag` parameters, includes `tag` in request body when using `MESSAGE_TAG`. ④ Updated `sendReminderWithQuickReplies` to use `MESSAGE_TAG` + `CONFIRMED_EVENT_UPDATE` instead of `UPDATE`. ⑤ Added fallback logging when messages fail due to window expiry (error code `1545041`) — logs warning and creates audit log entry via `createStaffNotification` so staff know to follow up manually (e.g., phone call). File: `src/lib/services/notification-service.ts`. TypeScript compiles cleanly. |
| UI-UX-REFACTOR-01 | Admin Portal UI Refactor & Fluid Motion System | 2026-08-30 11:50 UTC+08:00 | Complete visual design overhaul across all Admin Portal pages (`/dashboard`, `/appointments`, `/bookings`, `/check-in`, `/patients`, `/settings`, `/services`, `/audit`, `/chat`, `/billing`): ① Branded Hero Headers with cyan-to-teal gradient emblems (`bg-gradient-to-tr from-cyan-600 to-teal-500 text-white shadow-md shadow-cyan-500/20`). ② Executive Medical Cards with patient initials avatars (`MC`, `JD`), procedure badges, formatted date/time badges, and 2-column info grids. ③ Apple/Linear style compact segmented control tabs (`bg-slate-100 dark:bg-slate-800 p-1 rounded-xl w-fit`). ④ 0.9s Skeleton Shimmer Loading States (`loading.tsx`) across server routes (`/bookings`, `/dashboard`, `/patients`, `/billing`). ⑤ Smooth 200ms page entrance transitions (`animate-in fade-in-50 duration-200`) and tactile springy button press feedback (`active:scale-[0.97]`). ⑥ Patient Directory fixes (`/patients`): Initials avatar circles, calculated age (`36 yrs`), corrected allergy badges (active allergies show amber warning, "None" shows green check `No Known Allergies`), 1-click `View Profile` button, cleaned dropdown menu (`Edit Demographics`, `Archive Record`). ⑦ Edit Patient Modal fixes (`patient-form-dialog.tsx`): Added missing `useEffect` re-sync with `reset(patientData)` on open, fixed pre-filling existing patient data, and fixed clearing allergies to empty/null. TypeScript compiles cleanly (`npx tsc --noEmit` — 0 errors). |
| DEV-NOTE-LOGIN-01 | Temporary Quick Test Login Portal Helper Note | 2026-08-30 11:51 UTC+08:00 | **DEVELOPMENT DEMO FEATURE NOTICE**: The "QUICK TEST LOGIN" section on `/login` (`src/app/(auth)/login/login-form.tsx`) featuring 1-click login buttons for Admin (`admin@clinic.local`), Reception (`reception@clinic.local`), Dentist 1 (`dentist@clinic.local`), and Dentist 2 (`dentist2@clinic.local`) is **STRICTLY TEMPORARY FOR DEVELOPMENT & PREVIEW TESTING ONLY**. These quick login buttons allow rapid role-switching during development and will be **COMPLETELY REMOVED** prior to production deployment to enforce standard password authentication. |
| ROLE-GATE-01 | Role-Gated Admin Cards on Overview Dashboard | 2026-08-30 13:02 UTC+08:00 | Role-gated the "Admin Management Modules" (Settings, Audit, Services, Archive) and technical "System Status" (Supabase DB endpoint, RLS stats) cards on `/dashboard` (`dashboard-client.tsx`). Wrapped both `<Card>` elements in `{role === "admin" && (...)}` role-gate checks. When logged in as a Dentist or Receptionist, these admin widgets are completely hidden from the overview page layout. |
| NAV-PERM-01 | Dentist Portal Access Permissions & Sidebar Polish | 2026-08-30 13:18 UTC+08:00 | Fixed Dentist role access permissions & sidebar navigation: ① Added `"dentist"` to `ROLE_ROUTES` in `src/middleware.ts` and `NAV_ITEMS` in `src/lib/constants/navigation.ts` for `/dentists/unavailability` ("Schedule & Leave"), `/check-in`, and `/waitlist`. ② Auto-selected and locked the Dentist Account field on `/dentists/unavailability` (`unavailability-client.tsx`) when logged in as a doctor so dentists can only submit leave requests for themselves. ③ Fixed `UnauthorizedPage` (`page.tsx`) `nativeButton={false}` prop. ④ Removed left-edge indicator bar from active sidebar nav pills (`sidebar-nav.tsx`) for a clean rounded pill style. TypeScript compiles cleanly (`npx tsc --noEmit` — 0 errors). |
| ALLERGY-MULTI-01 | Multi-Allergy Comma Parsing & Badge Rendering System | 2026-08-30 17:48 UTC+08:00 | Created shared `parseAllergies()` utility helper in `src/lib/utils.ts` to split patient allergy inputs by comma, semicolon, or newline into clean individual allergen strings. Updated all patient views to render individual alert badges per allergen: ① Patient Directory (`patients-client.tsx`) — renders distinct amber warning badges per allergen (e.g. `⚠️ Fent`, `⚠️ Sample`). ② Patient Profile Detail View (`patient-detail-client.tsx`) — renders `Known Patient Allergies (N)` card with individual badges. ③ Consultation Workspace (`consultation-client.tsx`) — displays separate alert badges for clinical clarity. ④ Dentist Portal (`more-client.tsx`) — renders individual allergen pills on mobile. ⑤ Patient Form Dialog (`patient-form-dialog.tsx`) — updated placeholder and helper text to guide staff to separate multiple allergies with commas. TypeScript compiles cleanly (`npx tsc --noEmit` — 0 errors). |
| WORK-SHIFT-01 | Dentist Weekly Work Shift Hours Editor & RLS Permissions | 2026-08-30 18:00 UTC+08:00 | Implemented dentist weekly work hours management & RLS fixes: ① Added `getWeeklyScheduleAction` and `saveWeeklyScheduleAction` in `src/app/(dashboard)/dentists/unavailability/actions.ts` to manage `dentist_schedules` per doctor. ② Built interactive `Weekly Work Shift Hours` editor card on `/dentists/unavailability` (`unavailability-client.tsx`) allowing doctors to toggle active days and edit start/end work times (e.g. 08:00 AM – 05:00 PM) with 1-click save. ③ Created migration `20260830175500_fix_appointments_rls_for_dentist.sql` granting `dentist` role INSERT/UPDATE permissions on `appointments` and `appointment_services`. ④ Created migration `20260830180000_update_dentist_schedules_rls.sql` granting `reception` and `dentist` roles access to `dentist_schedules`. All migrations applied to database. TypeScript compiles cleanly (`npx tsc --noEmit` — 0 errors). |
| BUG-063 Fix | Timezone date filtering bug (UTC conversion shifts dates back 1 day in UTC+8) | 2026-08-31 17:25 UTC+08:00 | `new Date().toISOString().split("T")[0]` converts to UTC before formatting, causing dates to shift back by one day in Philippine timezone (UTC+8). Appointments on boundary dates (today, last day of month) were excluded from calendar, check-in, queue, dashboard stats, and other date-filtered queries. Created `src/lib/utils/date-utils.ts` with `todayLocal()`, `formatDateLocal()`, and `endOfMonthLocal()` utility functions that format dates in local timezone without UTC conversion. Replaced all 15+ instances across server pages, API routes, and client components: `appointments/page.tsx`, `check-in/page.tsx`, `consultation/page.tsx`, `dashboard/actions.ts`, `queue/page.tsx`, `queue/actions.ts`, `dentist-portal/emergency/actions.ts`, `dentist-portal/more/page.tsx`, `dentist-portal/queue/page.tsx`, `dentist-portal/schedule/page.tsx`, `api/check-in/search/route.ts`, `api/queue/route.ts`, `api/cron/reminders/route.ts`, `appointment-calendar.tsx`, `check-in-client.tsx`, `waitlist-client.tsx`, `appointment-form.tsx`, `follow-up-scheduler.tsx`. |
| BUG-064 Fix | Bookings "All Requests" tab shows 0 results (client-side filtering of pending-only server data) | 2026-08-31 17:40 UTC+08:00 | Bookings dashboard `BookingDashboardClient` used `useState(initialBookings)` which only initialized once with pending-only server data. Tab clicks only filtered client-side without triggering a server re-fetch. Three fixes applied: ① Added `"all"` to `validStatuses` array in `bookings/page.tsx` so server recognizes `?status=all` query param and fetches all appointments. ② Changed tab `onClick` in `booking-dashboard-client.tsx` to call `router.push('/bookings?status=${f.key}')` for server-side data fetch per filter selection. ③ Added `key={filterStatus}` prop to `BookingDashboardClient` in `bookings/page.tsx` to force remount with fresh server data when filter changes. Also fixed "View All Requests" button in empty state to navigate to `?status=all` via `router.push`. Rescheduled appointments now visible when "All Requests" or `?status=rescheduled` filter is selected. |
| BUG-065 Fix | QR codes RLS policy blocks dentist from generating QR codes | 2026-08-31 20:00 UTC+08:00 | RLS `qr_codes_insert` and `qr_codes_update` policies only allowed `reception` and `admin` roles, causing `new row violates row-level security policy for table "qr_codes"` when dentists generate QR codes. FR-23 states "staff" can generate QR codes. Created migration `20260901000000_fix_qr_codes_rls_for_all_staff.sql` — dropped and recreated both policies to include `'dentist'` alongside `'admin'` and `'reception'`. Applied via `npx supabase db reset --local`. Updated `docs/SECURITY.md` RLS policy summary table to reflect all staff roles. |
| BUG-066 Fix | Maximum update depth exceeded in patient-form-dialog.tsx + Dental/Medical History tabs not clickable | 2026-08-31 19:55 UTC+08:00 | `useEffect` in `PatientFormDialog` (`src/components/patients/patient-form-dialog.tsx:57`) had `conditionIds` (array prop defaulting to `[]`) in its dependency array, creating a new reference on every render and triggering an infinite loop of `reset()` calls. This froze the UI, making "Dental History" and "Medical History" tabs unclickable. Fix: Added `useRef(prevOpen)` to track previous `open` prop state. Modified `useEffect` to only execute `reset()` when `open` transitions from `false` to `true`. Verified in browser: all 3 tabs (Personal Info, Dental History, Medical History) are fully clickable, zero console errors. |
| BUG-067 Fix | QR code patient registration aligned with regular registration fields + UI/UX improvements | 2026-08-31 19:55 UTC+08:00 | QR code registration (`/register/[token]`) only captured 6 basic fields — missing dental history, medical questions, allergy checklist, and medical conditions. Rewrote `registration-wizard.tsx` to use `react-hook-form` with `patientSchema` and shared form section components (`PatientFormPersonalSection`, `PatientFormDentalHistorySection`, `PatientFormMedicalQuestionsSection`, `PatientFormAllergyConditionsSection`). Added 4-tab layout (Personal Info, Dental History, Medical History, Review) matching staff registration. Updated `submitRegistrationAction` in `actions.ts` to handle all patient schema fields including medical records and conditions. Added `getMedicalConditionsAction` to fetch conditions server-side. Updated `page.tsx` to fetch and pass conditions to wizard. UI/UX: cyan-themed design, progress bar, tab navigation, review summary with condition badges, privacy notice, success screen. TypeScript compiles cleanly (`npx tsc --noEmit` — 0 errors). |
| DENTAL-CHART-REDESIGN | Dental Chart Findings-Based Model Redesign | 2026-08-31 20:45 UTC+08:00 | Complete redesign of dental chart from single-finding-per-surface model to normalized multi-finding model. **Database**: Created migration `20260831210000_redesign_dental_chart_findings.sql` — 3 new tables (`tooth_presence`, `tooth_findings`, `finding_surfaces`), 3 new enum types (`tooth_presence_type`, `tooth_finding_category`, `tooth_surface`), RLS policies for all 3 tables, data migration from old `dental_chart_teeth` table (dropped after migration). **Types**: Updated `enums.ts` with `ToothPresenceStatus`, `ToothFindingCategory`, `ToothCondition`, `ToothRestoration`, `ToothSurgery` types. Updated `database.ts` with `ToothPresence`, `ToothFinding`, `FindingSurface` interfaces replacing `DentalChartTooth`. **Validation**: Updated `dental-chart.schema.ts` with `toothPresenceInputSchema`, `toothFindingInputSchema` supporting multi-surface arrays. **Service**: Rewrote `dental-chart-service.ts` with new CRUD methods (`upsertPresence`, `addFinding`, `updateFindingSurfaces`, `deleteFinding`, `deleteAllFindingsForTooth`); `getFullChart` returns `{ chart, presence, findings }`. **Server Actions**: Updated `dental-chart-actions.ts` with `upsertPresenceAction`, `addFindingAction`, `updateFindingSurfacesAction`, `deleteFindingAction`, `clearToothAction`. **UI Components**: ① `ToothEditor` — presence radio (single select), surface multi-select (M/D/B/L/O), multi-select conditions/restorations/surgery with `+` to add, existing findings list with per-finding delete, clear all button. ② `ToothIcon` — renders multiple finding codes per surface (e.g. `D/Am`), presence-based ring color. ③ `DentalChartGrid` — accepts `presence[]` and `findings[]` arrays, builds maps for lookup. ④ `DentalChartPanel` — wires new actions and data flow, supports `readOnly` mode for consultation. ⑤ `DentalChartLegend` — 4-column layout with Presence, Conditions, Restorations, Surgery. ⑥ `tooth-legend.ts` — added `PRESENCE_LEGEND`, `getFindingCode`, `getFindingColor`, `getPresenceColor`, `getPresenceCode`. **Pages Updated**: Patient detail page + client (`dentalChartPresence`, `dentalChartFindings` props), consultation page + client + treatment form (same new props, `readOnly` mode). **Verification**: TypeScript compiles with 0 errors. Browser test confirmed: added condition (Decayed) + restoration (Amalgam) coexisting on same tooth/surface, no console errors. |
| DENTAL-CHART-UX-FIX | Dental Chart Surface Selection UX Fix | 2026-08-31 21:03 UTC+08:00 | Fixed two UX issues in `ToothEditor` (`src/components/dental-chart/tooth-editor.tsx`): ① **Surface persistence** — surfaces were cleared after each finding add, forcing users to re-select surfaces for every condition/restoration/surgery. Removed `setSelectedSurfaces(new Set())` from `handleAddFinding` so surfaces persist, allowing rapid sequential finding additions to the same surfaces. ② **Optional surfaces** — surface selection was labeled "select before adding findings" implying it was required. Updated label to "optional — select to apply finding to specific surfaces" and added hint text "No surfaces selected — finding will apply to the whole tooth" when none are selected. Backend already supported empty surfaces via Zod schema default `[]`. |
| DENTAL-CHART-SURFACE-SYNC | Dental Chart Surface Selection Sync (SVG ↔ Editor) | 2026-08-31 21:14 UTC+08:00 | Lifted `selectedSurfaces` state from `ToothEditor` local state to `DentalChartPanel` shared state so that clicking a surface on the tooth SVG toggles it in the same Set as the ToothEditor's M/D/B/L/O buttons. Changes across 4 files: ① `dental-chart-panel.tsx` — replaced `selectedSurface` (single) with `selectedSurfaces` (Set), added `toggleSurface` function, `handleSurfaceClick` now toggles surfaces in shared state, passes `selectedSurfaces` + `onToggleSurface` to both grid and editor. ② `dental-chart-grid.tsx` — updated props from `selectedSurface` to `selectedSurfaces` (Set), passes through to `ToothIcon`. ③ `tooth-icon.tsx` — updated props from `selectedSurface` to `selectedSurfaces` (Set), highlight check changed from `===` to `.has()`. ④ `tooth-editor.tsx` — removed local `selectedSurfaces` state and `toggleSurface` function, now receives `selectedSurfaces` and `onToggleSurface` from props. TypeScript compiles with 0 errors. |
| SIDEBAR-COLLAPSE | Collapsible Desktop Sidebar (Icon-Only Mode) | 2026-08-31 21:25 UTC+08:00 | Added collapsible sidebar to desktop dashboard. Changes: ① `dashboard-shell.tsx` — added `sidebarCollapsed` state, sidebar width transitions from `w-64` (256px) to `w-16` (64px) with 200ms ease-in-out, logo text + role badge hidden when collapsed, passes `collapsed` prop to `SidebarNav` and `sidebarCollapsed` + `onToggleSidebar` to `Topbar`. ② `sidebar-nav.tsx` — accepts `collapsed` prop, shows icon-only when collapsed with hover tooltips (right side, 0ms delay via TooltipProvider), hides labels when collapsed. ③ `topbar.tsx` — accepts `sidebarCollapsed` and `onToggleSidebar` props, renders icon-only toggle button (PanelLeftClose/PanelLeftOpen) on the left side of the header (desktop-only, hidden on mobile). User name remains visible in topbar dropdown in both modes. TypeScript compiles with 0 errors. Browser-verified: expanded mode shows full labels, collapsed mode shows icon-only with tooltips, toggle works both ways. |
| 128 | BUG-068 Fix | Dentist Patient Registration RLS Policy | 2026-09-01 10:44 UTC+08:00 | Registering a new patient while logged in as a Dentist failed with RLS error `new row violates row-level security policy for table "patients"` because policy `patients_insert` only permitted `'reception'` and `'admin'` roles. Created migration `20260901003000_fix_patients_insert_rls_for_dentist.sql` updating `patients_insert` policy to `get_user_role() IN ('reception', 'dentist', 'admin')`. Executed query on local database. Verified dentist patient registration succeeds. |
| 129 | BUG-069 Fix | Zero-Amount Invoice Checkout & Record Payment Button Visibility | 2026-09-01 11:03 UTC+08:00 | Zero-amount invoices blocked checkout with "Cannot checkout — payment pending" because `generateInvoiceAction` set `payment_status: "pending_payment"` regardless of total amount, and `+ Record Payment` button was hidden when remaining balance was 0. Updated `generateInvoiceAction` to set `payment_status: "paid"` when `total_amount === 0`. Updated `checkoutAction` to permit checkout for ₱0 invoices. Updated `billing-client.tsx` to keep `+ Record Payment` button visible whenever payment form is closed. |
| 130 | BUG-070 Feature | One-Click Dental Chart Findings Summary in Billing & Invoices | 2026-09-01 10:59 UTC+08:00 | Staff lacked 1-click visibility into patient FDI dental chart findings when generating invoices on `/billing/[appointmentId]`. Created `getPatientDentalChartSummaryAction` in `actions.ts` querying `DentalChartService.getFullChart`. Added interactive **"Patient Dental Chart Findings Summary"** card with **"1-Click Copy Summary"** button in `billing-client.tsx`. |
| 131 | BUG-071 Feature | Reception Booking Pending Default & Verbal Dentist Pre-Approval Override | 2026-09-01 11:20 UTC+08:00 | All new appointments created by Reception automatically defaulted to `booking_status: "approved"`, bypassing dentist schedule review, with no mechanism for Reception to signal verbal doctor approval for walk-ins. Updated `appointment.schema.ts` and `appointment-service.ts` default `booking_status` to `"pending"`. Added `isVerballyApproved` checkbox toggle and dynamic status preview badge (`APPROVED IMMEDIATELY` vs `REQUIRES DENTIST APPROVAL`) to `appointment-form.tsx`. Updated `actions.ts` to set status based on verbal approval toggle. |
| 132 | BUG-072 Fix | Restricting Appointment Approval Authority to Dentists & Admins | 2026-09-01 11:26 UTC+08:00 | Reception staff had access to Approve and Decline action buttons on `/bookings`, violating clinical approval authority. Updated `bookings/page.tsx` to resolve `userRole` and pass to `BookingDashboardClient`. Updated `booking-dashboard-client.tsx` to hide Approve & Decline buttons for `reception` role and display `Awaiting Dentist Approval` badge. Updated `approveAppointmentAction` and `declineAppointmentAction` in `appointments/actions.ts` to reject non-dentist/non-admin approvals on the server. |
| 133 | BUG-073 & 074 | Invoice Service Price Evaluation Fix & Billing Desk Status Filter Tabs | 2026-09-01 11:56 UTC+08:00 | Fixed invoice bill totals showing ₱0.00 despite active services listing ₱500.00 due to `0 ?? 500` nullish coalescing evaluation bug and unpopulated `appointment_services.price` rows. Updated `AppointmentService.createAppointment` in `appointment-service.ts` to populate `appointment_services.price` from `dental_services.default_price`. Updated `generateInvoiceAction` & `getInvoiceAction` in `billing/actions.ts` to fall back to `default_price` if `price` is 0/null. Added status filter tabs (`Unpaid & Pending`, `Paid & Settled`, `All Records`) with live category counts to `/billing` (`billing-list-client.tsx`). Executed SQL update query on local database. |
| 134 | BUG-075 & 076 | Pop-Up Animations Upgrade & Doctor Dropdown UUID Label Fix | 2026-09-01 18:55 UTC+08:00 | Upgraded pop-up modals, sheets, and dropdown menus (`dialog.tsx`, `sheet.tsx`, `dropdown-menu.tsx`) with smooth 150ms GPU transitions, `.gpu-accelerated` CSS rules, and `bg-black/40` overlay. Fixed Selected Doctor dropdown on `/dentists/unavailability` displaying raw database UUID (`b0000000-0000-4000-8000-000000000001`) by explicitly formatting `SelectValue` with doctor full name and specialization in `unavailability-client.tsx`. |
| 135 | BUG-077 Fix | Disabling Payment Form for Fully Paid Invoices | 2026-09-01 19:03 UTC+08:00 | Payment entry form and `+ Record Payment` button remained open/accessible on settled invoices (`PAID` status / `Remaining Balance = ₱0.00`). Updated `billing-client.tsx` to hide `+ Record Payment` button and form when `remaining <= 0` or `paymentStatus === "paid"`, rendering green `Invoice Fully Settled` badge instead. Updated `recordPaymentAction` in `billing/[appointmentId]/actions.ts` to validate remaining balance and reject duplicate payment attempts on settled invoices. |
| 136 | BUG-078 Fix | Hiding Complete Visit & Checkout Button for Completed Visits | 2026-09-01 19:06 UTC+08:00 | `Complete Visit & Checkout` button remained visible on invoice header even after the visit was completed (`visit_status: "completed"`). Added `visitStatus` and `bookingStatus` to `InvoiceData` in `billing/[appointmentId]/actions.ts`. Updated `billing-client.tsx` to check `isVisitCompleted` and replace the button with a green `Visit Completed & Checked Out` badge once finished. |
| 137 | BUG-079 Feature | Digital Payment Proof Photo Upload & Mandatory Validation | 2026-09-01 19:12 UTC+08:00 | Added proof photo upload dropzone for digital payment options (GCash, Maya, Card, Bank Transfer) in `billing-client.tsx`, hiding it for Cash. Enforced mandatory photo upload before enabling `Confirm Payment` button on client UI and validated `proofImageUrl` in `recordPaymentAction` on server side. Added popup receipt viewer Dialog for viewing past digital receipts in billing history. Updated `billing/page.tsx` query to filter out completed & paid visits from active billing desk. |
| 138 | BUG-080 Fix | Alternate Dentist Reassignment Dropdown & Database Trigger Status Transition Fix | 2026-09-01 19:44 UTC+08:00 | Reassign Appointment dropdown on `/dentists/unavailability` displayed empty options and failed execution. Updated `findAlternateDentists` in `reassignment-service.ts` to list all active alternate doctors and fallback 30-min slots. Removed `!staffId` client blocking guard in `unavailability-client.tsx` and rendered in-modal `dialogError` alert box. Updated `validate_appointment_status()` PostgreSQL trigger to permit `approved -> rescheduled` transitions. |
