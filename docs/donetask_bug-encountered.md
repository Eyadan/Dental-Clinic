# Bugs Encountered — Dental Clinic Management System

> **Purpose:** Log every bug encountered during development. Updated after each task is completed or when a bug is found during QA.
> **Related:** [dev-order.md](./dev-order.md) · [plan.md](./plan.md) · [plan_done.md](./plan_done.md)

---

## How to Use This Document

1. **When a bug is found:** Add an entry to the table below with full details.
2. **When a bug is fixed:** Update `Status` to ✅ Fixed and add the fix description + date.
3. **Severity levels:** 🔴 Critical · 🟠 High · 🟡 Medium · 🟢 Low
4. **Status values:** 🐛 Open · 🔄 In Progress · ✅ Fixed · ❌ Won't Fix (with reason)

---

## Bug Log

| # | Date Found | Phase | Task # | Severity | Status | Bug Description | Steps to Reproduce | Expected Behavior | Actual Behavior | Fix Description | Date Fixed |
|---|---|---|---|---|---|---|---|---|---|---|---|
| BUG-001 | 2026-08-29 13:10 | 1 | P1-01 | 🟢 Low | ✅ Fixed | `ToothIcon` does not exist as an export from `lucide-react` | Import `{ ToothIcon }` from `lucide-react` and use in JSX | Icon renders | TypeScript error: `Module '"lucide-react"' has no exported member 'ToothIcon'` | Replaced with `Activity` icon from lucide-react | 2026-08-29 13:11 |
| BUG-002 | 2026-08-29 14:25 | 1 | P1-10 | 🟡 Medium | ✅ Fixed | shadcn/ui `asChild` prop not recognized by base-ui components | Use `<SheetTrigger asChild>` or `<DropdownMenuTrigger asChild>` with base-ui based shadcn components | Component renders child as trigger | TypeScript error: `Property 'asChild' does not exist on type 'IntrinsicAttributes & Props<unknown>'` | Replaced `asChild` with `render={<Component />}` prop (base-ui pattern) | 2026-08-29 14:28 |
| BUG-003 | 2026-08-29 14:55 | 2 | P2-04 | 🟡 Medium | ✅ Fixed | Zod schema with `.default()` causes RHF resolver type mismatch | Use `dentalServiceSchema` (with `is_active: z.boolean().default(true)`) as RHF resolver | Form types align between input and output | TypeScript error: `Type 'boolean | undefined' is not assignable to type 'boolean'` | Created separate `serviceFormSchema = dentalServiceSchema.omit({ is_active: true })` for form, keeping `is_active` server-side only | 2026-08-29 14:57 |
| BUG-004 | 2026-08-29 15:35 | 2 | P2-03b | 🟡 Medium | ✅ Fixed | base-ui Select `onValueChange` passes `string \| null`, incompatible with `Dispatch<SetStateAction<string>>` | Use `useState<string>` with Select's `onValueChange={setState}` | Select updates state correctly | TypeScript error: `Type 'string \| null' is not assignable to type 'SetStateAction<string>'` | Wrapped setState in handler functions: `const handleChange = (v: string \| null) => setState(v ?? "default")` | 2026-08-29 15:36 |
| BUG-005 | 2026-08-29 17:40 | 3 | P3-02 | 🟢 Low | ✅ Fixed | Incorrect type import `QRCode` instead of `QrCode` from database types | Import `QRCode` type from `@/lib/types/database` | Type matches Supabase generated types | TypeScript error: `Module has no exported member 'QRCode'` | Changed import to `QrCode` (matching the actual interface name in database.ts) | 2026-08-29 17:40 |
| BUG-006 | 2026-08-29 18:00 | 3 | P3-03 | 🟡 Medium | ✅ Fixed | `null` assigned to optional patient fields causes TypeScript strict mode error | Pass `null` for optional fields (email, birth_date, medical_history, allergies) to PatientService.createPatient | Patient created with null optional fields | TypeScript error: `Type 'null' is not assignable to type 'string \| undefined'` | Changed `null` to `undefined` for optional patient fields in submitRegistrationAction | 2026-08-29 18:00 |
| BUG-007 | 2026-08-29 17:20 | 3 | P3-01 | 🟢 Low | ✅ Fixed | Unused `isCheckingIn` state variable in CheckInClient | Declare `isCheckingIn` state with `useState` but never use it for rendering | No lint warning | ESLint/TS warning: unused variable | Removed `isCheckingIn` state and related setter calls | 2026-08-29 17:20 |
| BUG-008 | 2026-08-29 20:00 | 3 | P3-13 | 🟢 Low | ✅ Fixed | `Tooth` icon does not exist as export from `lucide-react` | Import `{ Tooth as ToothIcon }` from `lucide-react` and use in treatment form | Icon renders | TypeScript error: `Module '"lucide-react"' has no exported member 'Tooth'` | Replaced with `Activity` icon from lucide-react | 2026-08-29 20:00 |
| BUG-009 | 2026-08-29 20:00 | 3 | P3-13 | 🟡 Medium | ✅ Fixed | No `cn` utility function existed in the project for class name merging | Import `cn` from `@/lib/utils/theme` in dental-chart component | Utility function merges class names | TypeScript error: `Cannot find module '@/lib/utils/theme'` | Created `src/lib/utils/cn.ts` with simple `cn` function and updated import path | 2026-08-29 20:00 |
| BUG-010 | 2026-08-29 20:50 | 4 | P4-02 | 🟡 Medium | ✅ Fixed | `InvoiceData \| null \| undefined` not assignable to `InvoiceData \| null` in billing page | `getInvoiceAction` returns `ServiceResult<InvoiceData \| null>` which can be `undefined` | Invoice data passed to BillingClient | TypeScript error: `Type 'undefined' is not assignable to type 'InvoiceData \| null'` | Used null coalescing: `invoiceResult.data ?? null` with explicit type annotation | 2026-08-29 20:50 |
| BUG-011 | 2026-08-29 21:00 | 4 | P4-07 | 🟡 Medium | ✅ Fixed | `SchedulingService.checkConflict` called as static method but it's an instance method | Call `SchedulingService.checkConflict(supabase, ...)` directly | Conflict check works | TypeScript error: `Property 'checkConflict' does not exist on type 'typeof SchedulingService'` | Created instance: `new SchedulingService(supabase)` then called `schedulingService.checkConflict(...)` with correct params (date string + time string, not Date object) | 2026-08-29 21:00 |
| BUG-012 | 2026-08-29 22:50 | 5 | P5-02 | 🟢 Low | ✅ Fixed | Supabase join returns array for `users` relation, not single object — type cast failed | Cast `d.users` as `{ first_name: string; last_name: string }` directly | Dentist names extracted from join | TypeScript error: `Conversion of type '{ first_name: any; last_name: any; }[]' to type '{ first_name: string; last_name: string }' may be a mistake` | Used `Array.isArray(d.users) ? d.users[0] : d.users` pattern to handle both array and object forms | 2026-08-29 22:50 |
| BUG-013 | 2026-08-30 15:10 | 5 | P5-ENH | 🟠 High | ✅ Fixed | CSP `connect-src` blocks local Supabase auth requests (`http://127.0.0.1:54321`) | Start dev server, navigate to /login, enter credentials, submit form | Login succeeds, Supabase auth token retrieved | Console error: `Connecting to 'http://127.0.0.1:54321/auth/v1/token?grant_type=password' violates CSP directive "connect-src"` — login fails silently | Added `http://127.0.0.1:54321 ws://127.0.0.1:54321 http://localhost:54321 ws://localhost:54321` to `connect-src` in `next.config.ts` CSP headers | 2026-08-30 15:10 |
| BUG-014 | 2026-08-30 15:10 | 5 | P5-ENH | 🟠 High | ✅ Fixed | Middleware calls `updateSession` (Supabase auth) before checking if route is public — crashes on webhook routes | Facebook sends webhook verification GET to `/api/webhooks/messenger` | Webhook returns `hub.challenge` | 500 error: `Your project's URL and Key are required to create a Supabase client` — middleware crashes before route handler runs | Moved `isPublicRoute` check before `updateSession` call in `middleware.ts` — public routes now return `NextResponse.next()` immediately without Supabase | 2026-08-30 15:10 |
| BUG-015 | 2026-08-30 15:10 | 5 | P5-ENH | 🟡 Medium | ✅ Fixed | `.env.local` uses `NEXT_PUBLIC_SUPABASE_ANON_KEY` but code expects `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | Create `.env.local` with `NEXT_PUBLIC_SUPABASE_ANON_KEY=...` and start dev server | App reads Supabase key from env | Crash: `process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` is undefined — Supabase client creation fails | Renamed env var in `.env.local` from `NEXT_PUBLIC_SUPABASE_ANON_KEY` to `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | 2026-08-30 15:10 |
| BUG-016 | 2026-08-30 15:10 | 5 | P5-ENH | 🟡 Medium | ✅ Fixed | Seed data inserts into `users` table but doesn't create Supabase Auth accounts — no login credentials exist | Try to log in with seeded user emails | Login succeeds with seeded credentials | Auth error: `Invalid login credentials` — no auth.users entries exist | Created Supabase Auth users via admin API, then updated `users` table IDs and `dentists` table FKs to match auth user UUIDs | 2026-08-30 15:10 |
| BUG-017 | 2026-08-29 16:20 | 7 | P7-05/06 | 🟠 High | ✅ Fixed | Next.js build error: "You cannot have two parallel pages that resolve to the same path" — `(dentist)/queue` and `(dashboard)/queue` both resolve to `/queue` | Run `next build` with both `(dentist)` and `(dashboard)` route groups having `queue/page.tsx` | Build succeeds | Build error: parallel route conflict between `(dentist)` and `(dashboard)` groups | Moved all `(dentist)` pages into `(dentist)/dentist-portal/` subfolder so URLs become `/dentist-portal/queue`, `/dentist-portal/schedule`, etc. — no path collision with `(dashboard)` routes | 2026-08-29 16:25 |
| BUG-018 | 2026-08-29 17:08 | 7 | P2-05 | 🟡 Medium | ✅ Fixed | New Appointment form dentist dropdown shows license number instead of dentist name; Select components missing hidden inputs for FormData submission | Navigate to /appointments/new, open dentist dropdown | Dentist names (e.g. "John Doe · General Dentistry") shown in dropdown and trigger | Dropdown shows "PRC-D-12345 (General Dentistry)" — no human-readable name; Select value not submitted with FormData since Select is not a native form input | Updated `DentistService.getAllDentists()` to join `users` table and return `full_name` field. Updated `Dentist` interface with optional `full_name`. Updated `SelectValue` in `appointment-form.tsx` to display `full_name` for both trigger and dropdown items. Added `<input type="hidden" name="patient_id"/"dentist_id">` for FormData submission. Also fixed walk-in page to use `DentistService` instead of raw Supabase query. | 2026-08-29 17:08 |
| BUG-020 | 2026-08-29 17:13 | 7 | P2-05 | 🟡 Medium | ✅ Fixed | Dentist names still not displaying in appointment form — Supabase returns `users` join as array, not single object | Navigate to /appointments/new after BUG-018 fix, open dentist dropdown | Dentist names shown in dropdown | Dropdown shows empty string or fallback license_no — `full_name` is empty because `d.users` is `[{ first_name, last_name }]` (array) but code treats it as single object | Updated `getAllDentists()` to use `Array.isArray(rawUsers)` check — extracts `rawUsers[0]` if array, otherwise uses object directly. Same pattern as BUG-012 fix. | 2026-08-29 17:13 |
| BUG-021 | 2026-08-29 17:57 | 7 | P2-05 | 🟠 High | ✅ Fixed | Zod `.uuid()` rejects seed `users` and `dentists` fake UUIDs (version/variant `0000`) — appointment creation fails with "Invalid dentist ID" | Select a dentist and submit the New Appointment form | Appointment created successfully | Zod validation error: "Invalid dentist ID" (also affects patient ID if seeded with fake UUIDs) because `a0000000-0000-0000-0000-...` and `b0000000-0000-0000-0000-...` don't match RFC-4122 version/variant | Updated all hardcoded `users` and `dentists` IDs in `supabase/seed.sql` and `supabase/create_auth_users.sql` to valid v4 UUIDs (`...-0000-4000-8000-...`), preserving referential integrity. Ran `npx supabase db reset` and recreated auth users. | 2026-08-29 17:57 |
| BUG-022 | 2026-08-29 18:21 | 7 | P2-05 | 🟠 High | ✅ Fixed | RLS policy blocks admin from creating appointment — `get_user_role()` returns GoTrue `role` claim ('authenticated') instead of application role | Log in as admin and submit New Appointment form | Appointment is created and saved | Postgres error: `new row violates row-level security policy for table "appointments"` | Replaced `get_user_role()` implementation from `auth.jwt() ->> 'role'` to `SELECT role FROM public.users WHERE id = auth.uid()` with `SECURITY DEFINER`. Updated original RLS migration comment. Created and applied new migration `20260829182200_fix_get_user_role.sql`. | 2026-08-29 18:21 |
| BUG-024 | 2026-08-29 18:33 | 7 | P2-08 | 🟠 High | ✅ Fixed | Booking approve/decline fails — `log_appointment_history()` trigger cannot insert into `appointment_history` due to RLS | Click Approve or Decline on a pending booking in `/bookings` | Booking status is updated and audit row is logged | Postgres error: `new row violates row-level security policy for table "appointment_history"` — `appointment_history` has no INSERT policy, and the trigger runs as the invoker | Added `SECURITY DEFINER SET search_path = public` to `log_appointment_history()` trigger function so it inserts audit rows as the table owner, bypassing RLS. Created and applied migration `20260829183300_fix_appointment_history_trigger_rls.sql`. Updated original `create_triggers.sql` for future `db reset`. | 2026-08-29 18:33 |
| BUG-025 | 2026-08-29 18:43 | 7 | P3-04 | 🟠 High | ✅ Fixed | Queue page crashes: `column dentists_1.full_name does not exist` — query selects non-existent `full_name` from `dentists` table | Navigate to `/queue` as admin or reception | Queue page loads with patient and dentist names | Error: `Failed to fetch queue: column dentists_1.full_name does not exist` — `dentists` table has no `full_name` column; it's a computed field from `DentistService` joining `users` | Changed Supabase query from `dentists(full_name)` to `dentists(users(first_name, last_name))` in queue page, API route, and consultation page. Used `getSingleJoined()` for array/object normalisation with "Unknown Dentist" fallback. | 2026-08-29 18:43 |
| BUG-026 | 2026-08-29 18:50 | 7 | P3-07 | 🔴 Critical | ✅ Fixed | Queue Call/Delay actions crash: `invalid input value for enum visit_status: "no_show"` — trigger references `no_show` (a booking_status) as a visit_status | Click Call or Delay on a checked-in patient in `/queue` | Patient status updates to `waiting` or `delayed` | Error: `invalid input value for enum visit_status: "no_show"` — `validate_appointment_status()` trigger line `(OLD.visit_status = 'delayed' AND NEW.visit_status IN ('waiting', 'no_show'))` tries to cast `'no_show'` to `visit_status` enum which doesn't have that value | Removed `'no_show'` from the `delayed` transition in `validate_appointment_status()`. No-show is handled by setting `visit_status = NULL` which skips the transition check. Created and applied migration `20260829185000_fix_visit_status_trigger_enum.sql`. Updated original `create_triggers.sql`. Also improved error messages in queue actions to surface actual DB errors. | 2026-08-29 18:50 |
| BUG-023 | 2026-08-29 18:31 | 7 | P1-11 | 🟡 Medium | ✅ Fixed | Topbar user dropdown crashes: `MenuGroupContext is missing` — `DropdownMenuLabel` used outside `DropdownMenuGroup` | Log in and click the admin user avatar in the topbar | Dropdown opens showing user name, email, and Sign out | Console error: `Base UI: MenuGroupContext is missing. Menu group parts must be used within <Menu.Group> or <Menu.RadioGroup>` — dropdown content crashes | Wrapped `DropdownMenuLabel` and `DropdownMenuItem` in `<DropdownMenuGroup>` in `topbar.tsx`. Base UI's `MenuPrimitive.GroupLabel` requires a `Menu.Group` ancestor context. | 2026-08-29 18:31 |
| BUG-019 | 2026-08-29 17:08 | 7 | P1-11 | 🟡 Medium | ✅ Fixed | Next.js build prerender error: `useSearchParams()` should be wrapped in a suspense boundary at page "/login" | Run `next build` — static page generation fails on /login | Build succeeds with /login prerendered | Build error: `useSearchParams() should be wrapped in a suspense boundary at page "/login"` — CSR bailout during static generation | Extracted login form into `login-form.tsx` client component (takes `redirectUrl` prop, no `useSearchParams`). Converted `page.tsx` to async server component reading `searchParams` prop, wrapping `LoginForm` in `<Suspense>`. | 2026-08-29 17:08 |
| BUG-027 | 2026-08-30 16:30 | 7 | P5-10 | 🟡 Medium | ✅ Fixed | `dismissStaffNotificationAction` fails to DELETE from `audit_logs` — table has only SELECT RLS policy (immutable audit trail), no DELETE policy | Click Dismiss on a failed Messenger notification alert in dashboard | Notification is dismissed and removed from list | RLS error: `new row violates row-level security policy` — DELETE denied on `audit_logs` | Changed `dismissStaffNotificationAction` to use `createServiceRoleClient()` (admin client) which bypasses RLS, allowing DELETE on `audit_logs` for dismissed notification alerts only. | 2026-08-30 16:30 |
| BUG-028 | 2026-08-30 16:35 | 7 | P4-02 | 🟡 Medium | ✅ Fixed | Billing `getInvoiceAction` accesses `patients` and `dentists.users` as single objects without `getSingleJoined` — Supabase returns arrays for joins | Navigate to `/billing/[appointmentId]` for an appointment with invoice | Patient and dentist names display correctly | Names show "Unknown" or crash because Supabase returns `[{ first_name, last_name }]` (array) but code accesses `.first_name` directly | Added `getSingleJoined()` calls for patient and dentist user data extraction in `getInvoiceAction`. Import `getSingleJoined` from `@/lib/utils/supabase-join`. | 2026-08-30 16:35 |
| BUG-029 | 2026-08-30 16:40 | 7 | P4-07 | 🔴 Critical | ✅ Fixed | `createFollowUpAction` missing `scheduled_date` (NOT NULL column), sets `visit_status: "checked_in"` for future appointment, inserts full ISO datetime into `time without time zone` column | Schedule a follow-up appointment from billing page | Follow-up appointment created successfully | Postgres error: NOT NULL violation on `scheduled_date`. Also, `visit_status: "checked_in"` makes follow-up appear in queue immediately. `scheduled_time` receives full ISO string instead of time-only string. | Added `scheduled_date: scheduledDate` to insert. Removed `visit_status: "checked_in"` (defaults to NULL — patient hasn't arrived). Changed `scheduled_time` from `scheduledDateTime.toISOString()` to `scheduledTime` (time-only string). Also surfaced actual DB error in error message. | 2026-08-30 16:40 |
| BUG-030 | 2026-08-30 16:45 | 7 | P6-03 | 🟡 Medium | ✅ Fixed | Dentist portal queue, schedule, and more pages access `patients` join as single object without `getSingleJoined` — Supabase returns arrays | Navigate to dentist portal `/dentist-portal/queue`, `/schedule`, or `/more` | Patient names display correctly | Names crash or show undefined because `appt.patients` is `[{ first_name, last_name }]` (array) but code accesses `.first_name` directly | Added `getSingleJoined()` calls for patient data extraction in all three dentist portal pages (queue, schedule, more). Import `getSingleJoined` from `@/lib/utils/supabase-join`. | 2026-08-30 16:45 |
| BUG-031 | 2026-08-30 16:50 | 7 | P4-08 | 🟢 Low | ✅ Fixed | `checkoutAction` hides actual DB error with generic "Failed to complete visit" message | Attempt checkout when visit_status is not `checkout` (e.g. still `treatment_ongoing`) | User sees specific error explaining the invalid status transition | User sees generic "Failed to complete visit" — no indication of what went wrong | Changed error message from generic string to `apptError.message` to surface actual database error (e.g. trigger violation message). | 2026-08-30 16:50 |
| BUG-032 | 2026-08-30 19:40 | 7 | P7-08 | 🟡 Medium | ✅ Fixed | Open redirect vulnerability in login `redirect` query param — attacker can craft `/login?redirect=https://evil.com` | Visit `/login?redirect=https://evil.com` and log in | User is redirected to `/dashboard` (safe default) | User is redirected to `https://evil.com` (attacker-controlled site) | Added `isSafeRedirect()` validation in `login/page.tsx` — redirect URL must start with `/` and not start with `//` (protocol-relative). Falls back to `/dashboard` if invalid. | 2026-08-30 19:40 |
| BUG-033 | 2026-08-30 19:45 | 7 | P7-08 | 🟢 Low | ✅ Fixed | Cron API routes (`/api/cron/reminders`, `/api/cron/expiration`) use `!==` for CRON_SECRET comparison instead of `timingSafeEqual` — vulnerable to timing attacks | Send crafted Bearer token to cron endpoint | Timing-safe comparison prevents timing-based secret recovery | `!==` comparison leaks token length/content via timing differences | Added `verifyCronAuth()` helper using `timingSafeEqual` from `crypto` module in both cron routes. Length check before comparison to prevent errors. | 2026-08-30 19:45 |
| BUG-034 | 2026-08-30 19:48 | 7 | P7-08 | 🟢 Low | ✅ Fixed | API routes (`/api/check-in/search`, `/api/queue`) lack explicit `auth.getUser()` checks — rely only on middleware + RLS | Call API route without auth cookie (e.g. curl) | 401 JSON response `{"error":"Unauthorized"}` | Middleware returns HTML redirect to `/login` (not JSON), RLS returns empty data | Added explicit `auth.getUser()` check at top of both API routes, returning 401 JSON if no user. Defense-in-depth alongside middleware + RLS. | 2026-08-30 19:48 |
| BUG-035 | 2026-08-30 20:10 | 7 | P4-02 | 🟠 High | ✅ Fixed | Missing index pages for `/billing`, `/consultation`, `/consent` — sidebar nav links to these routes but only dynamic `[id]` pages exist, causing 404 "Page Not Found" | Click Billing, Consultation, or Consent in sidebar | Page loads with list of relevant records | 404 "Page Not Found" — no `page.tsx` at root of these route directories | Created index pages: `billing/page.tsx` (lists appointments with invoices + payment status), `consultation/page.tsx` (lists today's patients ready for consultation), `consent/page.tsx` (lists all consent forms with signed/pending status). Each has a client component with search/filter and links to detail pages. | 2026-08-30 20:10 |
| BUG-036 | 2026-08-30 20:30 | 7 | P7-08 | 🟠 High | ✅ Fixed | Middleware RBAC blocks admin from `/consultation` and `/consent` — `ROLE_ROUTES` mapped these to `"dentist"` only, but UI/UX spec §6.1 and navigation.ts include admin access | Log in as admin, click Consultation or Consent in sidebar | Admin sees consultation/consent list page | Admin redirected to `/unauthorized` page | Changed `ROLE_ROUTES` from `Record<string, UserRole>` (single role) to `Record<string, UserRole[]>` (multiple roles). Updated `/consultation` and `/consent` to `["admin", "dentist"]`. Updated `getRequiredRole()` → `getAllowedRoles()` returning array, middleware checks `allowedRoles.includes(role)`. | 2026-08-30 20:30 |
| BUG-037 | 2026-08-30 20:35 | 7 | P7-08 | 🟠 High | ✅ Fixed | Middleware missing role guards for 5 routes — `/check-in`, `/chat`, `/dentists/unavailability`, `/waitlist`, `/patients/archived` had no `ROLE_ROUTES` entry, allowing any authenticated user to access them regardless of role | Log in as dentist, navigate to `/check-in` or `/chat` or `/patients/archived` | Dentist redirected to `/unauthorized` (nav doesn't show these for dentist) | Dentist can access these pages directly via URL — no middleware guard | Added all 5 routes to `ROLE_ROUTES`: `/check-in` → `["admin", "reception"]`, `/chat` → `["admin", "reception"]`, `/dentists/unavailability` → `["admin", "reception"]`, `/waitlist` → `["admin", "reception"]`, `/patients/archived` → `["admin"]`. Now middleware enforces role-based access matching the navigation config. | 2026-08-30 20:35 |
| BUG-038 | 2026-08-30 21:00 | 8 | P8-01 | 🔴 Critical | ✅ Fixed | New patients cannot book via Messenger — `finalizeBooking` in `booking-parser.ts` requires existing patient record (matched by `messenger_psid`) to create appointment. If no record found, bot tells patient to call clinic, blocking the entire PRD flow. | Send "book" from a new Messenger user (no patient record) | Bot collects date/time/service/dentist, creates pending appointment, staff reviews it | Bot refuses: "I couldn't find your patient record. Please call the clinic to register first" | Added `findOrCreatePlaceholderPatient()` — creates minimal patient record with PSID and placeholder name. `finalizeBooking` now always succeeds for any Messenger user. Registration flow via QR code updates the placeholder patient instead of creating a duplicate. | 2026-08-30 21:00 |
| ARCH-001 | 2026-08-30 21:00 | 8 | P8-01 | 🟠 High | ✅ Fixed | Booking sessions stored in-memory (`Map`) — won't survive server restarts, won't work in serverless deployments (Vercel), each function invocation gets fresh instance | Start booking conversation, restart dev server, send next message | Session persists across restarts | Session lost — bot doesn't recognize conversation state | Created `booking_sessions` table with migration. Replaced in-memory `Map` with DB-backed `getSession()`/`saveSession()`/`deleteSession()` functions. Sessions auto-expire after 30 minutes via query filter. | 2026-08-30 21:00 |
| BUG-039 | 2026-08-30 21:20 | 8 | P8-01 | 🔴 Critical | ✅ Fixed | Booking approval/decline does not send Messenger notification to patient — `approveAppointmentAction` and `declineAppointmentAction` in `appointments/actions.ts` only update the DB status but never call `sendNotification()`. PRD §2.1 requires "Auto-notifications to patients via Messenger on approval/decline". | Book via Messenger, staff approves the booking on Booking Dashboard | Patient receives "✅ Your appointment has been approved!" message on Messenger | Patient receives nothing — booking status changes to "approved" but patient is not notified | Added `sendNotification()` calls to both actions. Approve sends `"approval"` type with date/time/dentist name. Decline sends `"decline"` type with reference number. Also added `appointment_history` and `audit_logs` entries for both actions (were missing). | 2026-08-30 21:20 |
| ARCH-002 | 2026-08-30 21:30 | 8 | P5-06 | 🟠 High | ✅ Fixed | Live Chat Dashboard uses polling instead of Supabase Realtime, and unread indicator counts all inbound messages instead of only unread ones — no `is_read` column on `messenger_messages` | Open Live Chat, send message from Messenger, wait for update | New messages appear instantly via WebSocket, unread badge shows only unread messages | Messages appear after 5-10s polling delay, unread count never resets | Added `is_read` column to `messenger_messages` (migration). Fixed `unread_count` query to filter `is_read = false`. Added `markAsReadAction` called on conversation select. Replaced `setInterval` polling in `chat-client.tsx` and `chat-thread.tsx` with Supabase Realtime `postgres_changes` subscriptions. Conversation list updates on any conversation/message change. Chat thread updates on INSERT to its conversation. | 2026-08-30 21:30 |
| BUG-040 | 2026-08-30 21:35 | 8 | P5-06 | 🟠 High | ✅ Fixed | Live Chat staff messages fail with `(#100) Cannot tag messages with "HUMAN_AGENT" without prior approval` — `sendStaffMessage` used `MESSAGE_TAG` with `HUMAN_AGENT` tag which requires Facebook Page approval | Staff takes over chat, types message, sends | Message delivered to patient's Messenger | Facebook API rejects: `(#100) Cannot tag messages with "HUMAN_AGENT" without prior approval` | Changed `sendStaffMessage` from `sendToMessenger(psid, text, "MESSAGE_TAG", "HUMAN_AGENT")` to `sendToMessenger(psid, text, "RESPONSE")`. The `RESPONSE` type works within 24h of the user's last message, which covers the active conversation scenario. No Facebook approval needed. | 2026-08-30 21:35 |
| BUG-041 | 2026-08-30 21:45 | 8 | P3-01 | 🟠 High | ✅ Fixed | Check-In page shows only a search bar with no list of today's appointments — reception staff can't see who's scheduled today without typing a search query. PRD §2.3 requires "Staff checks in patients on arrival" and dev-order QA says "Selecting patient shows appointment for today" | Open Check-In page | Today's approved/confirmed appointments listed with patient name, ref, time, status, check-in button, and QR link | Empty page with only a search bar — no appointments visible | Made `check-in/page.tsx` a server component that fetches today's approved/confirmed appointments ordered by time. Passes them as `initialAppointments` to `CheckInClient`. Client shows them on load with "Today's Appointments (N)" header. Search bar filters — when empty, shows all today's appointments; when 2+ chars, uses API search. Added QR code link button per appointment. | 2026-08-30 21:45 |
| BUG-065 | 2026-08-31 20:00 | 9 | P3-02 | 🟠 High | ✅ Fixed | QR code generation fails with `new row violates row-level security policy for table "qr_codes"` when a dentist generates a QR code for patient registration. FR-23 says "staff" can generate QR codes, but RLS `qr_codes_insert` policy only allowed `reception` and `admin` roles. | Log in as dentist, navigate to `/check-in/qr/[appointmentId]`, click "Generate QR Code" | QR code generates successfully with token and countdown timer | Postgres RLS error: `new row violates row-level security policy for table "qr_codes"` — insert denied because `get_user_role()` returns `'dentist'` which is not in the policy's allowed roles | Created migration `20260901000000_fix_qr_codes_rls_for_all_staff.sql` — dropped and recreated `qr_codes_insert` and `qr_codes_update` policies to include `'dentist'` alongside `'admin'` and `'reception'`. Applied via `npx supabase db reset --local`. | 2026-08-31 20:00 |
| BUG-066 | 2026-08-31 19:55 | 9 | P2-02 | 🔴 Critical | ✅ Fixed | "Maximum update depth exceeded" error in `patient-form-dialog.tsx:57` — `useEffect` dependency array included `conditionIds` (an array prop defaulting to `[]`), creating a new reference on every render and triggering an infinite loop of `setState` calls via `reset()`. This also caused the "Dental History" and "Medical History" tabs to be unclickable (UI frozen by infinite re-renders). | Open patient registration dialog (`/patients` → "Register New Patient" or "Edit Demographics"), observe console | Dialog opens, tabs are clickable, no console errors | Console floods with "Maximum update depth exceeded" errors, dialog UI is frozen, tabs (Dental History, Medical History) do not respond to clicks | Added `useRef(prevOpen)` to track the previous `open` prop state. Modified `useEffect` to only execute `reset()` logic when `open` transitions from `false` to `true`, preventing re-runs while the dialog is open. File: `src/components/patients/patient-form-dialog.tsx` lines 55-63. | 2026-08-31 19:55 |
| BUG-067 | 2026-08-31 19:55 | 9 | P3-03 | 🟠 High | ✅ Fixed | QR code patient registration (`/register/[token]`) only captured 6 basic fields (first_name, last_name, contact_no, email, birth_date, medical_history, allergies) — missing all dental history, medical questions, allergy checklist, and medical conditions fields that the staff registration form captures. | Scan QR code, fill out registration form | All patient data fields captured (personal info, dental history, medical history, allergies, conditions) matching staff registration | Only 6 basic fields captured — no dental history, no medical questions, no allergy checklist, no medical conditions | Rewrote `registration-wizard.tsx` to use `react-hook-form` with `patientSchema` and shared form section components (`PatientFormPersonalSection`, `PatientFormDentalHistorySection`, `PatientFormMedicalQuestionsSection`, `PatientFormAllergyConditionsSection`). Added 4-tab layout (Personal Info, Dental History, Medical History, Review) matching staff registration. Updated `actions.ts` `submitRegistrationAction` to handle all patient schema fields. Added `getMedicalConditionsAction` to fetch conditions. Updated `page.tsx` to fetch and pass conditions. Files: `src/app/(public)/register/[token]/registration-wizard.tsx`, `src/app/(public)/register/[token]/actions.ts`, `src/app/(public)/register/[token]/page.tsx`. | 2026-08-31 19:55 |
# Bugs Encountered — Dental Clinic Management System

> **Purpose:** Log every bug encountered during development. Updated after each task is completed or when a bug is found during QA.
> **Related:** [dev-order.md](./dev-order.md) · [plan.md](./plan.md) · [plan_done.md](./plan_done.md)

---

## How to Use This Document

1. **When a bug is found:** Add an entry to the table below with full details.
2. **When a bug is fixed:** Update `Status` to ✅ Fixed and add the fix description + date.
3. **Severity levels:** 🔴 Critical · 🟠 High · 🟡 Medium · 🟢 Low
4. **Status values:** 🐛 Open · 🔄 In Progress · ✅ Fixed · ❌ Won't Fix (with reason)

---

## Bug Log

| # | Date Found | Phase | Task # | Severity | Status | Bug Description | Steps to Reproduce | Expected Behavior | Actual Behavior | Fix Description | Date Fixed |
|---|---|---|---|---|---|---|---|---|---|---|---|
| BUG-001 | 2026-08-29 13:10 | 1 | P1-01 | 🟢 Low | ✅ Fixed | `ToothIcon` does not exist as an export from `lucide-react` | Import `{ ToothIcon }` from `lucide-react` and use in JSX | Icon renders | TypeScript error: `Module '"lucide-react"' has no exported member 'ToothIcon'` | Replaced with `Activity` icon from lucide-react | 2026-08-29 13:11 |
| BUG-002 | 2026-08-29 14:25 | 1 | P1-10 | 🟡 Medium | ✅ Fixed | shadcn/ui `asChild` prop not recognized by base-ui components | Use `<SheetTrigger asChild>` or `<DropdownMenuTrigger asChild>` with base-ui based shadcn components | Component renders child as trigger | TypeScript error: `Property 'asChild' does not exist on type 'IntrinsicAttributes & Props<unknown>'` | Replaced `asChild` with `render={<Component />}` prop (base-ui pattern) | 2026-08-29 14:28 |
| BUG-003 | 2026-08-29 14:55 | 2 | P2-04 | 🟡 Medium | ✅ Fixed | Zod schema with `.default()` causes RHF resolver type mismatch | Use `dentalServiceSchema` (with `is_active: z.boolean().default(true)`) as RHF resolver | Form types align between input and output | TypeScript error: `Type 'boolean | undefined' is not assignable to type 'boolean'` | Created separate `serviceFormSchema = dentalServiceSchema.omit({ is_active: true })` for form, keeping `is_active` server-side only | 2026-08-29 14:57 |
| BUG-004 | 2026-08-29 15:35 | 2 | P2-03b | 🟡 Medium | ✅ Fixed | base-ui Select `onValueChange` passes `string \| null`, incompatible with `Dispatch<SetStateAction<string>>` | Use `useState<string>` with Select's `onValueChange={setState}` | Select updates state correctly | TypeScript error: `Type 'string \| null' is not assignable to type 'SetStateAction<string>'` | Wrapped setState in handler functions: `const handleChange = (v: string \| null) => setState(v ?? "default")` | 2026-08-29 15:36 |
| BUG-005 | 2026-08-29 17:40 | 3 | P3-02 | 🟢 Low | ✅ Fixed | Incorrect type import `QRCode` instead of `QrCode` from database types | Import `QRCode` type from `@/lib/types/database` | Type matches Supabase generated types | TypeScript error: `Module has no exported member 'QRCode'` | Changed import to `QrCode` (matching the actual interface name in database.ts) | 2026-08-29 17:40 |
| BUG-006 | 2026-08-29 18:00 | 3 | P3-03 | 🟡 Medium | ✅ Fixed | `null` assigned to optional patient fields causes TypeScript strict mode error | Pass `null` for optional fields (email, birth_date, medical_history, allergies) to PatientService.createPatient | Patient created with null optional fields | TypeScript error: `Type 'null' is not assignable to type 'string \| undefined'` | Changed `null` to `undefined` for optional patient fields in submitRegistrationAction | 2026-08-29 18:00 |
| BUG-007 | 2026-08-29 17:20 | 3 | P3-01 | 🟢 Low | ✅ Fixed | Unused `isCheckingIn` state variable in CheckInClient | Declare `isCheckingIn` state with `useState` but never use it for rendering | No lint warning | ESLint/TS warning: unused variable | Removed `isCheckingIn` state and related setter calls | 2026-08-29 17:20 |
| BUG-008 | 2026-08-29 20:00 | 3 | P3-13 | 🟢 Low | ✅ Fixed | `Tooth` icon does not exist as export from `lucide-react` | Import `{ Tooth as ToothIcon }` from `lucide-react` and use in treatment form | Icon renders | TypeScript error: `Module '"lucide-react"' has no exported member 'Tooth'` | Replaced with `Activity` icon from lucide-react | 2026-08-29 20:00 |
| BUG-009 | 2026-08-29 20:00 | 3 | P3-13 | 🟡 Medium | ✅ Fixed | No `cn` utility function existed in the project for class name merging | Import `cn` from `@/lib/utils/theme` in dental-chart component | Utility function merges class names | TypeScript error: `Cannot find module '@/lib/utils/theme'` | Created `src/lib/utils/cn.ts` with simple `cn` function and updated import path | 2026-08-29 20:00 |
| BUG-010 | 2026-08-29 20:50 | 4 | P4-02 | 🟡 Medium | ✅ Fixed | `InvoiceData \| null \| undefined` not assignable to `InvoiceData \| null` in billing page | `getInvoiceAction` returns `ServiceResult<InvoiceData \| null>` which can be `undefined` | Invoice data passed to BillingClient | TypeScript error: `Type 'undefined' is not assignable to type 'InvoiceData \| null'` | Used null coalescing: `invoiceResult.data ?? null` with explicit type annotation | 2026-08-29 20:50 |
| BUG-011 | 2026-08-29 21:00 | 4 | P4-07 | 🟡 Medium | ✅ Fixed | `SchedulingService.checkConflict` called as static method but it's an instance method | Call `SchedulingService.checkConflict(supabase, ...)` directly | Conflict check works | TypeScript error: `Property 'checkConflict' does not exist on type 'typeof SchedulingService'` | Created instance: `new SchedulingService(supabase)` then called `schedulingService.checkConflict(...)` with correct params (date string + time string, not Date object) | 2026-08-29 21:00 |
| BUG-012 | 2026-08-29 22:50 | 5 | P5-02 | 🟢 Low | ✅ Fixed | Supabase join returns array for `users` relation, not single object — type cast failed | Cast `d.users` as `{ first_name: string; last_name: string }` directly | Dentist names extracted from join | TypeScript error: `Conversion of type '{ first_name: any; last_name: any; }[]' to type '{ first_name: string; last_name: string }' may be a mistake` | Used `Array.isArray(d.users) ? d.users[0] : d.users` pattern to handle both array and object forms | 2026-08-29 22:50 |
| BUG-013 | 2026-08-30 15:10 | 5 | P5-ENH | 🟠 High | ✅ Fixed | CSP `connect-src` blocks local Supabase auth requests (`http://127.0.0.1:54321`) | Start dev server, navigate to /login, enter credentials, submit form | Login succeeds, Supabase auth token retrieved | Console error: `Connecting to 'http://127.0.0.1:54321/auth/v1/token?grant_type=password' violates CSP directive "connect-src"` — login fails silently | Added `http://127.0.0.1:54321 ws://127.0.0.1:54321 http://localhost:54321 ws://localhost:54321` to `connect-src` in `next.config.ts` CSP headers | 2026-08-30 15:10 |
| BUG-014 | 2026-08-30 15:10 | 5 | P5-ENH | 🟠 High | ✅ Fixed | Middleware calls `updateSession` (Supabase auth) before checking if route is public — crashes on webhook routes | Facebook sends webhook verification GET to `/api/webhooks/messenger` | Webhook returns `hub.challenge` | 500 error: `Your project's URL and Key are required to create a Supabase client` — middleware crashes before route handler runs | Moved `isPublicRoute` check before `updateSession` call in `middleware.ts` — public routes now return `NextResponse.next()` immediately without Supabase | 2026-08-30 15:10 |
| BUG-015 | 2026-08-30 15:10 | 5 | P5-ENH | 🟡 Medium | ✅ Fixed | `.env.local` uses `NEXT_PUBLIC_SUPABASE_ANON_KEY` but code expects `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | Create `.env.local` with `NEXT_PUBLIC_SUPABASE_ANON_KEY=...` and start dev server | App reads Supabase key from env | Crash: `process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` is undefined — Supabase client creation fails | Renamed env var in `.env.local` from `NEXT_PUBLIC_SUPABASE_ANON_KEY` to `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` | 2026-08-30 15:10 |
| BUG-016 | 2026-08-30 15:10 | 5 | P5-ENH | 🟡 Medium | ✅ Fixed | Seed data inserts into `users` table but doesn't create Supabase Auth accounts — no login credentials exist | Try to log in with seeded user emails | Login succeeds with seeded credentials | Auth error: `Invalid login credentials` — no auth.users entries exist | Created Supabase Auth users via admin API, then updated `users` table IDs and `dentists` table FKs to match auth user UUIDs | 2026-08-30 15:10 |
| BUG-017 | 2026-08-29 16:20 | 7 | P7-05/06 | 🟠 High | ✅ Fixed | Next.js build error: "You cannot have two parallel pages that resolve to the same path" — `(dentist)/queue` and `(dashboard)/queue` both resolve to `/queue` | Run `next build` with both `(dentist)` and `(dashboard)` route groups having `queue/page.tsx` | Build succeeds | Build error: parallel route conflict between `(dentist)` and `(dashboard)` groups | Moved all `(dentist)` pages into `(dentist)/dentist-portal/` subfolder so URLs become `/dentist-portal/queue`, `/dentist-portal/schedule`, etc. — no path collision with `(dashboard)` routes | 2026-08-29 16:25 |
| BUG-018 | 2026-08-29 17:08 | 7 | P2-05 | 🟡 Medium | ✅ Fixed | New Appointment form dentist dropdown shows license number instead of dentist name; Select components missing hidden inputs for FormData submission | Navigate to /appointments/new, open dentist dropdown | Dentist names (e.g. "John Doe · General Dentistry") shown in dropdown and trigger | Dropdown shows "PRC-D-12345 (General Dentistry)" — no human-readable name; Select value not submitted with FormData since Select is not a native form input | Updated `DentistService.getAllDentists()` to join `users` table and return `full_name` field. Updated `Dentist` interface with optional `full_name`. Updated `SelectValue` in `appointment-form.tsx` to display `full_name` for both trigger and dropdown items. Added `<input type="hidden" name="patient_id"/"dentist_id">` for FormData submission. Also fixed walk-in page to use `DentistService` instead of raw Supabase query. | 2026-08-29 17:08 |
| BUG-020 | 2026-08-29 17:13 | 7 | P2-05 | 🟡 Medium | ✅ Fixed | Dentist names still not displaying in appointment form — Supabase returns `users` join as array, not single object | Navigate to /appointments/new after BUG-018 fix, open dentist dropdown | Dentist names shown in dropdown | Dropdown shows empty string or fallback license_no — `full_name` is empty because `d.users` is `[{ first_name, last_name }]` (array) but code treats it as single object | Updated `getAllDentists()` to use `Array.isArray(rawUsers)` check — extracts `rawUsers[0]` if array, otherwise uses object directly. Same pattern as BUG-012 fix. | 2026-08-29 17:13 |
| BUG-021 | 2026-08-29 17:57 | 7 | P2-05 | 🟠 High | ✅ Fixed | Zod `.uuid()` rejects seed `users` and `dentists` fake UUIDs (version/variant `0000`) — appointment creation fails with "Invalid dentist ID" | Select a dentist and submit the New Appointment form | Appointment created successfully | Zod validation error: "Invalid dentist ID" (also affects patient ID if seeded with fake UUIDs) because `a0000000-0000-0000-0000-...` and `b0000000-0000-0000-0000-...` don't match RFC-4122 version/variant | Updated all hardcoded `users` and `dentists` IDs in `supabase/seed.sql` and `supabase/create_auth_users.sql` to valid v4 UUIDs (`...-0000-4000-8000-...`), preserving referential integrity. Ran `npx supabase db reset` and recreated auth users. | 2026-08-29 17:57 |
| BUG-022 | 2026-08-29 18:21 | 7 | P2-05 | 🟠 High | ✅ Fixed | RLS policy blocks admin from creating appointment — `get_user_role()` returns GoTrue `role` claim ('authenticated') instead of application role | Log in as admin and submit New Appointment form | Appointment is created and saved | Postgres error: `new row violates row-level security policy for table "appointments"` | Replaced `get_user_role()` implementation from `auth.jwt() ->> 'role'` to `SELECT role FROM public.users WHERE id = auth.uid()` with `SECURITY DEFINER`. Updated original RLS migration comment. Created and applied new migration `20260829182200_fix_get_user_role.sql`. | 2026-08-29 18:21 |
| BUG-024 | 2026-08-29 18:33 | 7 | P2-08 | 🟠 High | ✅ Fixed | Booking approve/decline fails — `log_appointment_history()` trigger cannot insert into `appointment_history` due to RLS | Click Approve or Decline on a pending booking in `/bookings` | Booking status is updated and audit row is logged | Postgres error: `new row violates row-level security policy for table "appointment_history"` — `appointment_history` has no INSERT policy, and the trigger runs as the invoker | Added `SECURITY DEFINER SET search_path = public` to `log_appointment_history()` trigger function so it inserts audit rows as the table owner, bypassing RLS. Created and applied migration `20260829183300_fix_appointment_history_trigger_rls.sql`. Updated original `create_triggers.sql` for future `db reset`. | 2026-08-29 18:33 |
| BUG-025 | 2026-08-29 18:43 | 7 | P3-04 | 🟠 High | ✅ Fixed | Queue page crashes: `column dentists_1.full_name does not exist` — query selects non-existent `full_name` from `dentists` table | Navigate to `/queue` as admin or reception | Queue page loads with patient and dentist names | Error: `Failed to fetch queue: column dentists_1.full_name does not exist` — `dentists` table has no `full_name` column; it's a computed field from `DentistService` joining `users` | Changed Supabase query from `dentists(full_name)` to `dentists(users(first_name, last_name))` in queue page, API route, and consultation page. Used `getSingleJoined()` for array/object normalisation with "Unknown Dentist" fallback. | 2026-08-29 18:43 |
| BUG-026 | 2026-08-29 18:50 | 7 | P3-07 | 🔴 Critical | ✅ Fixed | Queue Call/Delay actions crash: `invalid input value for enum visit_status: "no_show"` — trigger references `no_show` (a booking_status) as a visit_status | Click Call or Delay on a checked-in patient in `/queue` | Patient status updates to `waiting` or `delayed` | Error: `invalid input value for enum visit_status: "no_show"` — `validate_appointment_status()` trigger line `(OLD.visit_status = 'delayed' AND NEW.visit_status IN ('waiting', 'no_show'))` tries to cast `'no_show'` to `visit_status` enum which doesn't have that value | Removed `'no_show'` from the `delayed` transition in `validate_appointment_status()`. No-show is handled by setting `visit_status = NULL` which skips the transition check. Created and applied migration `20260829185000_fix_visit_status_trigger_enum.sql`. Updated original `create_triggers.sql`. Also improved error messages in queue actions to surface actual DB errors. | 2026-08-29 18:50 |
| BUG-023 | 2026-08-29 18:31 | 7 | P1-11 | 🟡 Medium | ✅ Fixed | Topbar user dropdown crashes: `MenuGroupContext is missing` — `DropdownMenuLabel` used outside `DropdownMenuGroup` | Log in and click the admin user avatar in the topbar | Dropdown opens showing user name, email, and Sign out | Console error: `Base UI: MenuGroupContext is missing. Menu group parts must be used within <Menu.Group> or <Menu.RadioGroup>` — dropdown content crashes | Wrapped `DropdownMenuLabel` and `DropdownMenuItem` in `<DropdownMenuGroup>` in `topbar.tsx`. Base UI's `MenuPrimitive.GroupLabel` requires a `Menu.Group` ancestor context. | 2026-08-29 18:31 |
| BUG-019 | 2026-08-29 17:08 | 7 | P1-11 | 🟡 Medium | ✅ Fixed | Next.js build prerender error: `useSearchParams()` should be wrapped in a suspense boundary at page "/login" | Run `next build` — static page generation fails on /login | Build succeeds with /login prerendered | Build error: `useSearchParams() should be wrapped in a suspense boundary at page "/login"` — CSR bailout during static generation | Extracted login form into `login-form.tsx` client component (takes `redirectUrl` prop, no `useSearchParams`). Converted `page.tsx` to async server component reading `searchParams` prop, wrapping `LoginForm` in `<Suspense>`. | 2026-08-29 17:08 |
| BUG-027 | 2026-08-30 16:30 | 7 | P5-10 | 🟡 Medium | ✅ Fixed | `dismissStaffNotificationAction` fails to DELETE from `audit_logs` — table has only SELECT RLS policy (immutable audit trail), no DELETE policy | Click Dismiss on a failed Messenger notification alert in dashboard | Notification is dismissed and removed from list | RLS error: `new row violates row-level security policy` — DELETE denied on `audit_logs` | Changed `dismissStaffNotificationAction` to use `createServiceRoleClient()` (admin client) which bypasses RLS, allowing DELETE on `audit_logs` for dismissed notification alerts only. | 2026-08-30 16:30 |
| BUG-028 | 2026-08-30 16:35 | 7 | P4-02 | 🟡 Medium | ✅ Fixed | Billing `getInvoiceAction` accesses `patients` and `dentists.users` as single objects without `getSingleJoined` — Supabase returns arrays for joins | Navigate to `/billing/[appointmentId]` for an appointment with invoice | Patient and dentist names display correctly | Names show "Unknown" or crash because Supabase returns `[{ first_name, last_name }]` (array) but code accesses `.first_name` directly | Added `getSingleJoined()` calls for patient and dentist user data extraction in `getInvoiceAction`. Import `getSingleJoined` from `@/lib/utils/supabase-join`. | 2026-08-30 16:35 |
| BUG-029 | 2026-08-30 16:40 | 7 | P4-07 | 🔴 Critical | ✅ Fixed | `createFollowUpAction` missing `scheduled_date` (NOT NULL column), sets `visit_status: "checked_in"` for future appointment, inserts full ISO datetime into `time without time zone` column | Schedule a follow-up appointment from billing page | Follow-up appointment created successfully | Postgres error: NOT NULL violation on `scheduled_date`. Also, `visit_status: "checked_in"` makes follow-up appear in queue immediately. `scheduled_time` receives full ISO string instead of time-only string. | Added `scheduled_date: scheduledDate` to insert. Removed `visit_status: "checked_in"` (defaults to NULL — patient hasn't arrived). Changed `scheduled_time` from `scheduledDateTime.toISOString()` to `scheduledTime` (time-only string). Also surfaced actual DB error in error message. | 2026-08-30 16:40 |
| BUG-030 | 2026-08-30 16:45 | 7 | P6-03 | 🟡 Medium | ✅ Fixed | Dentist portal queue, schedule, and more pages access `patients` join as single object without `getSingleJoined` — Supabase returns arrays | Navigate to dentist portal `/dentist-portal/queue`, `/schedule`, or `/more` | Patient names display correctly | Names crash or show undefined because `appt.patients` is `[{ first_name, last_name }]` (array) but code accesses `.first_name` directly | Added `getSingleJoined()` calls for patient data extraction in all three dentist portal pages (queue, schedule, more). Import `getSingleJoined` from `@/lib/utils/supabase-join`. | 2026-08-30 16:45 |
| BUG-031 | 2026-08-30 16:50 | 7 | P4-08 | 🟢 Low | ✅ Fixed | `checkoutAction` hides actual DB error with generic "Failed to complete visit" message | Attempt checkout when visit_status is not `checkout` (e.g. still `treatment_ongoing`) | User sees specific error explaining the invalid status transition | User sees generic "Failed to complete visit" — no indication of what went wrong | Changed error message from generic string to `apptError.message` to surface actual database error (e.g. trigger violation message). | 2026-08-30 16:50 |
| BUG-032 | 2026-08-30 19:40 | 7 | P7-08 | 🟡 Medium | ✅ Fixed | Open redirect vulnerability in login `redirect` query param — attacker can craft `/login?redirect=https://evil.com` | Visit `/login?redirect=https://evil.com` and log in | User is redirected to `/dashboard` (safe default) | User is redirected to `https://evil.com` (attacker-controlled site) | Added `isSafeRedirect()` validation in `login/page.tsx` — redirect URL must start with `/` and not start with `//` (protocol-relative). Falls back to `/dashboard` if invalid. | 2026-08-30 19:40 |
| BUG-033 | 2026-08-30 19:45 | 7 | P7-08 | 🟢 Low | ✅ Fixed | Cron API routes (`/api/cron/reminders`, `/api/cron/expiration`) use `!==` for CRON_SECRET comparison instead of `timingSafeEqual` — vulnerable to timing attacks | Send crafted Bearer token to cron endpoint | Timing-safe comparison prevents timing-based secret recovery | `!==` comparison leaks token length/content via timing differences | Added `verifyCronAuth()` helper using `timingSafeEqual` from `crypto` module in both cron routes. Length check before comparison to prevent errors. | 2026-08-30 19:45 |
| BUG-034 | 2026-08-30 19:48 | 7 | P7-08 | 🟢 Low | ✅ Fixed | API routes (`/api/check-in/search`, `/api/queue`) lack explicit `auth.getUser()` checks — rely only on middleware + RLS | Call API route without auth cookie (e.g. curl) | 401 JSON response `{"error":"Unauthorized"}` | Middleware returns HTML redirect to `/login` (not JSON), RLS returns empty data | Added explicit `auth.getUser()` check at top of both API routes, returning 401 JSON if no user. Defense-in-depth alongside middleware + RLS. | 2026-08-30 19:48 |
| BUG-035 | 2026-08-30 20:10 | 7 | P4-02 | 🟠 High | ✅ Fixed | Missing index pages for `/billing`, `/consultation`, `/consent` — sidebar nav links to these routes but only dynamic `[id]` pages exist, causing 404 "Page Not Found" | Click Billing, Consultation, or Consent in sidebar | Page loads with list of relevant records | 404 "Page Not Found" — no `page.tsx` at root of these route directories | Created index pages: `billing/page.tsx` (lists appointments with invoices + payment status), `consultation/page.tsx` (lists today's patients ready for consultation), `consent/page.tsx` (lists all consent forms with signed/pending status). Each has a client component with search/filter and links to detail pages. | 2026-08-30 20:10 |
| BUG-036 | 2026-08-30 20:30 | 7 | P7-08 | 🟠 High | ✅ Fixed | Middleware RBAC blocks admin from `/consultation` and `/consent` — `ROLE_ROUTES` mapped these to `"dentist"` only, but UI/UX spec §6.1 and navigation.ts include admin access | Log in as admin, click Consultation or Consent in sidebar | Admin sees consultation/consent list page | Admin redirected to `/unauthorized` page | Changed `ROLE_ROUTES` from `Record<string, UserRole>` (single role) to `Record<string, UserRole[]>` (multiple roles). Updated `/consultation` and `/consent` to `["admin", "dentist"]`. Updated `getRequiredRole()` → `getAllowedRoles()` returning array, middleware checks `allowedRoles.includes(role)`. | 2026-08-30 20:30 |
| BUG-037 | 2026-08-30 20:35 | 7 | P7-08 | 🟠 High | ✅ Fixed | Middleware missing role guards for 5 routes — `/check-in`, `/chat`, `/dentists/unavailability`, `/waitlist`, `/patients/archived` had no `ROLE_ROUTES` entry, allowing any authenticated user to access them regardless of role | Log in as dentist, navigate to `/check-in` or `/chat` or `/patients/archived` | Dentist redirected to `/unauthorized` (nav doesn't show these for dentist) | Dentist can access these pages directly via URL — no middleware guard | Added all 5 routes to `ROLE_ROUTES`: `/check-in` → `["admin", "reception"]`, `/chat` → `["admin", "reception"]`, `/dentists/unavailability` → `["admin", "reception"]`, `/waitlist` → `["admin", "reception"]`, `/patients/archived` → `["admin"]`. Now middleware enforces role-based access matching the navigation config. | 2026-08-30 20:35 |
| BUG-038 | 2026-08-30 21:00 | 8 | P8-01 | 🔴 Critical | ✅ Fixed | New patients cannot book via Messenger — `finalizeBooking` in `booking-parser.ts` requires existing patient record (matched by `messenger_psid`) to create appointment. If no record found, bot tells patient to call clinic, blocking the entire PRD flow. | Send "book" from a new Messenger user (no patient record) | Bot collects date/time/service/dentist, creates pending appointment, staff reviews it | Bot refuses: "I couldn't find your patient record. Please call the clinic to register first" | Added `findOrCreatePlaceholderPatient()` — creates minimal patient record with PSID and placeholder name. `finalizeBooking` now always succeeds for any Messenger user. Registration flow via QR code updates the placeholder patient instead of creating a duplicate. | 2026-08-30 21:00 |
| ARCH-001 | 2026-08-30 21:00 | 8 | P8-01 | 🟠 High | ✅ Fixed | Booking sessions stored in-memory (`Map`) — won't survive server restarts, won't work in serverless deployments (Vercel), each function invocation gets fresh instance | Start booking conversation, restart dev server, send next message | Session persists across restarts | Session lost — bot doesn't recognize conversation state | Created `booking_sessions` table with migration. Replaced in-memory `Map` with DB-backed `getSession()`/`saveSession()`/`deleteSession()` functions. Sessions auto-expire after 30 minutes via query filter. | 2026-08-30 21:00 |
| BUG-039 | 2026-08-30 21:20 | 8 | P8-01 | 🔴 Critical | ✅ Fixed | Booking approval/decline does not send Messenger notification to patient — `approveAppointmentAction` and `declineAppointmentAction` in `appointments/actions.ts` only update the DB status but never call `sendNotification()`. PRD §2.1 requires "Auto-notifications to patients via Messenger on approval/decline". | Book via Messenger, staff approves the booking on Booking Dashboard | Patient receives "✅ Your appointment has been approved!" message on Messenger | Patient receives nothing — booking status changes to "approved" but patient is not notified | Added `sendNotification()` calls to both actions. Approve sends `"approval"` type with date/time/dentist name. Decline sends `"decline"` type with reference number. Also added `appointment_history` and `audit_logs` entries for both actions (were missing). | 2026-08-30 21:20 |
| ARCH-002 | 2026-08-30 21:30 | 8 | P5-06 | 🟠 High | ✅ Fixed | Live Chat Dashboard uses polling instead of Supabase Realtime, and unread indicator counts all inbound messages instead of only unread ones — no `is_read` column on `messenger_messages` | Open Live Chat, send message from Messenger, wait for update | New messages appear instantly via WebSocket, unread badge shows only unread messages | Messages appear after 5-10s polling delay, unread count never resets | Added `is_read` column to `messenger_messages` (migration). Fixed `unread_count` query to filter `is_read = false`. Added `markAsReadAction` called on conversation select. Replaced `setInterval` polling in `chat-client.tsx` and `chat-thread.tsx` with Supabase Realtime `postgres_changes` subscriptions. Conversation list updates on any conversation/message change. Chat thread updates on INSERT to its conversation. | 2026-08-30 21:30 |
| BUG-040 | 2026-08-30 21:35 | 8 | P5-06 | 🟠 High | ✅ Fixed | Live Chat staff messages fail with `(#100) Cannot tag messages with "HUMAN_AGENT" without prior approval` — `sendStaffMessage` used `MESSAGE_TAG` with `HUMAN_AGENT` tag which requires Facebook Page approval | Staff takes over chat, types message, sends | Message delivered to patient's Messenger | Facebook API rejects: `(#100) Cannot tag messages with "HUMAN_AGENT" without prior approval` | Changed `sendStaffMessage` from `sendToMessenger(psid, text, "MESSAGE_TAG", "HUMAN_AGENT")` to `sendToMessenger(psid, text, "RESPONSE")`. The `RESPONSE` type works within 24h of the user's last message, which covers the active conversation scenario. No Facebook approval needed. | 2026-08-30 21:35 |
| BUG-041 | 2026-08-30 21:45 | 8 | P3-01 | 🟠 High | ✅ Fixed | Check-In page shows only a search bar with no list of today's appointments — reception staff can't see who's scheduled today without typing a search query. PRD §2.3 requires "Staff checks in patients on arrival" and dev-order QA says "Selecting patient shows appointment for today" | Open Check-In page | Today's approved/confirmed appointments listed with patient name, ref, time, status, check-in button, and QR link | Empty page with only a search bar — no appointments visible | Made `check-in/page.tsx` a server component that fetches today's approved/confirmed appointments ordered by time. Passes them as `initialAppointments` to `CheckInClient`. Client shows them on load with "Today's Appointments (N)" header. Search bar filters — when empty, shows all today's appointments; when 2+ chars, uses API search. Added QR code link button per appointment. | 2026-08-30 21:45 |
| BUG-042 | 2026-08-30 21:50 | 8 | P3-02 | 🔴 Critical | ✅ Fixed | QR code generates URL with query param (`/register?token=...`) but the route is a dynamic segment (`/register/[token]`) — patient scans QR and gets 404 "Page Not Found" | Scan QR code, open URL on phone | Registration form loads | 404 "Page Not Found" — URL format doesn't match Next.js dynamic route | Changed `qr-display.tsx` from `${registrationUrl}?token=${token}` to `${registrationUrl}/${token}`. Now generates `/register/57a354b9-...` matching the `[token]` dynamic route. | 2026-08-30 21:50 |
| BUG-043 | 2026-08-30 22:00 | 8 | P3-02 | 🔴 Critical | ✅ Fixed | Public registration page always shows "Invalid QR Code" even with a valid, non-expired token — `validateTokenAction` and `submitRegistrationAction` use `createServerSupabaseClient` which reads auth session cookies, but patients scanning QR on their phone have no auth session. RLS on `qr_codes` requires `authenticated` role, so the query returns no rows. | Generate QR code, scan on phone (incognito), open URL | Registration form loads with patient info | "Invalid QR Code — This QR code is invalid, expired, or has already been used" | Replaced `createServerSupabaseClient` with a service role client (`createClient` with `SUPABASE_SERVICE_ROLE_KEY` and `persistSession: false`) in both `validateTokenAction` and `submitRegistrationAction`. Service role bypasses RLS, allowing public access to validate QR tokens and update patient records without auth session. | 2026-08-30 22:00 |
| BUG-044 | 2026-08-30 22:10 | 8 | P4-02 | 🟠 High | ✅ Fixed | Dentist cannot generate invoices — RLS `invoices_insert` policy only allows `reception` and `admin` roles, but UI/UX §6.1 grants billing access to dentists too. Dentist opens billing page, clicks generate invoice, gets RLS error. | Log in as dentist, open billing for an appointment, click "Generate Invoice" | Invoice created successfully | "new row violates row-level security policy for table invoices" | Created migration `20260830000000_fix_billing_rls_for_dentist.sql` — updated `invoices_insert`, `invoices_update`, `payments_insert`, `payments_update` RLS policies to include `dentist` role. Applied directly to local DB. | 2026-08-30 22:10 |
| BUG-045 | 2026-08-30 22:15 | 8 | P4-01 | 🟠 High | ✅ Fixed | Consent form generation fails with `null value in column "staff_id" of relation "consent_forms" violates not-null constraint` — `generateConsentAction` in `consultation/[appointmentId]/actions.ts` doesn't include `staff_id` in the insert, but the `consent_forms` table has `staff_id UUID NOT NULL` | Dentist opens consultation, enters treatment info, clicks "Generate Consent" | Consent form created successfully | "null value in column staff_id of relation consent_forms violates not-null constraint" | Added `supabase.auth.getUser()` to fetch current user ID and included `staff_id: user.id` in the `consent_forms` insert. | 2026-08-30 22:15 |
| BUG-046 | 2026-08-30 22:20 | 8 | P3-02 | 🟠 High | ✅ Fixed | Registration shows "Invalid QR Code" after successful submission — `submitRegistrationAction` calls `revalidatePath("/check-in")` after invalidating the QR token, which triggers Next.js to re-render the server component page, re-running `validateTokenAction` on the now-used token | Fill registration form, submit | "Registration Complete!" success screen | "Invalid QR Code — This QR code is invalid, expired, or has already been used" | Removed `revalidatePath("/check-in")` from both code paths in `submitRegistrationAction`. The client-side `setSuccess(true)` handles showing the success screen without server re-render. | 2026-08-30 22:20 |
| BUG-047 | 2026-08-30 22:25 | 8 | P3-01 | 🟠 High | ✅ Fixed | Check-In page shows "Invalid visit_status transition: in_consultation -> checked_in" error when patient is already past check-in stage — Check In button only disabled for `checked_in` status, not for other active statuses like `in_consultation`, `checkout`, etc. | Patient in consultation, reception clicks Check In | Button disabled with status label | Error: "Invalid visit_status transition: in_consultation -> checked_in" | Changed button `disabled` condition from `appt.visit_status === "checked_in"` to `appt.visit_status !== null`. Updated button label to show current status (Checked In, In Consultation, Checked Out, etc.) for any non-null visit_status. | 2026-08-30 22:25 |
| BUG-048 | 2026-08-30 23:40 | 5 | P5-ENH-2 | 🟠 High | ✅ Fixed | System messages (chat takeover/handoff notifications) not appearing in Live Chat web UI | Staff takes over chat, system notification message sent via `sendStaffMessage` — message appears in patient's Messenger but not in web chat UI | System notification appears in both patient Messenger and web chat UI | `sendStaffMessage` only saves to DB on successful Messenger API call | Added fallback in `takeChatAction` and `endChatAction`: after `sendStaffMessage` returns, if `sendResult.success` is false, save message directly to DB via `saveMessage(conversationId, "outbound", message)`. Conditional save avoids duplicates when Messenger send succeeds. | 2026-08-30 23:45 |
| BUG-049 | 2026-08-30 23:50 | 5 | P5-ENH-3 | 🟠 High | ✅ Fixed | No spam protection on Messenger webhook — users can flood bot with unlimited messages, create unlimited appointments, and send oversized messages | Send 100+ messages/second to bot webhook, create unlimited pending appointments by repeatedly booking | Rate limiting rejects excessive messages, appointment spam limit prevents unlimited bookings | Webhook handler had no rate limiting, no message length check, no duplicate detection, and `finalizeBooking` had no limit on active appointments per patient | Added 4 spam prevention measures: ① Rate limiting (5 msgs/min per PSID via in-memory Map), ② Message length limit (500 chars max), ③ Duplicate `message.mid` detection (Set with FIFO eviction, last 100), ④ Appointment spam limit (max 3 active pending/approved/confirmed appointments per patient). | 2026-08-30 23:55 |
| BUG-050 | 2026-08-31 00:00 | 5 | P5-ENH-4 | 🟠 High | ✅ Fixed | Appointment reminders and notifications silently fail when patient hasn't messaged bot within 24 hours — Facebook 24h messaging window restriction | Patient books appointment, doesn't message bot for 24+ hours, cron sends reminder via `sendNotification` with `UPDATE` type | Reminder delivered to patient's Messenger | Facebook API rejects with error 1545041 (messaging window closed) — `UPDATE` type still requires 24h window to be open | Changed `sendNotification` and `sendReminderWithQuickReplies` to use `MESSAGE_TAG` type with `CONFIRMED_EVENT_UPDATE` tag for reminder, follow_up, reschedule, and cancellation notification types. This tag allows sending outside 24h window for up to 7 days. Added fallback audit log entry via `createStaffNotification` when window expiry is detected so staff can follow up by phone. | 2026-08-31 00:05 |
| BUG-051 | 2026-08-30 10:35 | 8 | P1-01 | 🔴 Critical | 🐛 Open | Login form relies on Next.js soft navigation (`router.push('/dashboard')`) after setting auth cookies via Supabase client, causing browser to navigate before cookies update on the server request, resulting in an instant redirect loop back to `/login` | Enter valid credentials on `/login` and click "Sign in" | User is authenticated and redirected to `/dashboard` | Button changes to "Signing in...", then reverts to `/login` with no error message displayed | Recommended solution: Replace `router.push(redirectUrl)` with `window.location.href = redirectUrl` in `src/app/(auth)/login/login-form.tsx` to force hard navigation with updated session cookies. | N/A |
| BUG-052 | 2026-08-30 11:45 | 9 | P2-01 | 🟠 High | ✅ Fixed | Edit Patient Modal does not pre-fill existing patient data when opened, and clearing allergies fails to update record to "No Known Allergies" | Open Patient Directory `/patients`, click "Edit Demographics" on any patient (e.g. Maria Santos) | Form fields automatically pre-fill with patient's existing details, and clearing allergies sets status to "No Known Allergies" | Form fields showed stale default values ("Juan Dela Cruz") requiring re-typing, and clearing allergies text did not update form reset state | `PatientFormDialog` was missing a `useEffect` hook to call `reset(patientData)` when the modal opens or `patient` prop changes. Added `useEffect` with `reset({...})` in `patient-form-dialog.tsx` and updated `handleSubmit` in `patients-client.tsx` to update local state and trigger `router.refresh()`. | 2026-08-30 11:46 |
| BUG-053 | 2026-08-30 12:35 | 9 | P2-02 | 🟠 High | ✅ Fixed | Edit Dental Service Modal does not pre-fill existing procedure details when opened — form fields show default blank values requiring full re-typing | Open Dental Services `/services`, click Edit on any procedure (e.g. Tooth Extraction) | Form fields automatically pre-fill with procedure name, description, duration, and default price | Form fields showed blank default values requiring full re-typing | `ServiceFormDialog` was missing a `useEffect` hook to call `reset(serviceData)` when the modal opens or `service` prop changes. Added `useEffect` with `reset({...})` in `service-form-dialog.tsx` and updated `handleSubmit` in `services-client.tsx` to update local state and trigger `router.refresh()`. | 2026-08-30 12:36 |
| BUG-054 | 2026-08-30 13:00 | 9 | P2-03 | 🟠 High | ✅ Fixed | Overview Dashboard (`/dashboard`) shows "Admin Management Modules" and technical "System Status" cards to non-admin accounts (Dentists & Receptionists) | Log in as Dentist (`dentist@clinic.local`), view `/dashboard` | Admin module links (Settings, Audit, Services, Archive) and DB System Status cards hidden for non-admin accounts | Non-admin dentist accounts saw admin quick links and technical DB connection stats on overview dashboard | Wrapped both "Admin Management Modules" and "System Status" `<Card>` components in strict `{role === "admin" && (...)}` role-gate checks in `src/app/(dashboard)/dashboard/dashboard-client.tsx`. Verified cards hide for dentists and display for admins. | 2026-08-30 13:02 |
| BUG-055 | 2026-08-30 13:10 | 9 | P2-04 | 🟠 High | ✅ Fixed | Dentist role cannot access `/dentists/unavailability`, `/waitlist`, or `/check-in` — middleware blocked access to `/unauthorized`, sidebar nav omitted links for dentists, and unavailability page allowed choosing other doctors | Log in as Dentist (`dentist@clinic.local`), click "Schedule & Leave" | Dentist accesses leave management for their own account, and sidebar navigation includes Check-In, Schedule & Leave, and Waitlist | Middleware redirected dentist to `/unauthorized`, sidebar nav omitted links, and dentist select dropdown allowed picking other doctors | Added `"dentist"` to `ROLE_ROUTES` in `src/middleware.ts` and `NAV_ITEMS` in `src/lib/constants/navigation.ts`. Updated `UnavailabilityClient` to auto-select and lock leave requests to the logged-in doctor's account. Removed left-edge bar from active sidebar nav pills in `sidebar-nav.tsx`. | 2026-08-30 13:18 |
| BUG-056 | 2026-08-30 17:45 | 9 | P2-05 | 🟠 High | ✅ Fixed | Multiple patient allergies (e.g. `Fent, Sample`) rendered as a single merged badge string (`⚠️ Fent, Sample`) instead of individual warning badges per allergen | Enter `Fent, Sample` in patient allergies input, view Patient Directory `/patients` or Patient Profile `/patients/[id]` | Each allergen renders as a separate, distinct warning pill badge (`⚠️ Fent`, `⚠️ Sample`) | Comma-separated string rendered as one single combined badge | Added `parseAllergies()` helper in `src/lib/utils.ts` to split allergies by comma/semicolon/newline. Updated `patients-client.tsx`, `patient-detail-client.tsx`, `consultation-client.tsx`, `patient-form-dialog.tsx`, and `more-client.tsx` to render individual alert badges for each allergen. | 2026-08-30 17:48 |
| BUG-057 | 2026-08-30 17:52 | 9 | P2-06 | 🟠 High | ✅ Fixed | Creating an appointment while logged in as a Dentist failed with RLS error `new row violates row-level security policy for table "appointments"` | Log in as Dentist (`dentist2@clinic.local`), book new appointment at `/appointments/new` | Appointment is created and saved cleanly | Postgres RLS error rejected insert because `appointments_insert` and `appointment_services_insert` policies only allowed `reception` and `admin` | Created migration `20260830175500_fix_appointments_rls_for_dentist.sql` — updated `appointments_insert`, `appointment_services_insert`, `appointment_services_update`, and `appointment_services_delete` policies to include `dentist` role. Applied migration to database. | 2026-08-30 17:54 |
| BUG-058 | 2026-08-30 17:58 | 9 | P2-07 | 🟠 High | ✅ Fixed | Dentists had no visual interface to view and edit their weekly work shift hours (start/end times per day of week) on the web dashboard | Open `/dentists/unavailability`, attempt to adjust weekly working hours | Dentists can toggle working days and edit start/end times per day with 1-click Save | Only date-range unavailability block form existed, with no weekly shift editor | Added `getWeeklyScheduleAction` & `saveWeeklyScheduleAction` in `actions.ts`. Added interactive `Weekly Work Shift Hours` card in `unavailability-client.tsx`. Created migration `20260830180000_update_dentist_schedules_rls.sql` granting `reception` and `dentist` roles access to `dentist_schedules`. | 2026-08-30 18:00 |
| BUG-059 | 2026-08-30 18:16 | 9 | P2-08 | 🟠 High | ✅ Fixed | Declaring unavailability failed with `null value in column "changed_by" of relation "appointment_history" violates not-null constraint` when client state `staffId` was unpopulated | Open `/dentists/unavailability`, fill leave form, click "Declare Unavailability" | Leave block is saved cleanly and affected appointments are marked for reschedule | Server action relied on client-passed `staffId`; if empty, passed empty/null to DB trigger creating audit history | Updated `declareUnavailabilityAction` and `reassignAppointmentAction` in `src/app/(dashboard)/dentists/unavailability/actions.ts` to automatically resolve `effectiveStaffId` from `supabase.auth.getUser()` on the server side if `staffId` is empty or missing. | 2026-08-30 18:17 |
| BUG-060 | 2026-08-30 18:19 | 9 | P2-09 | 🔴 Critical | ✅ Fixed | Updating appointment status during leave declaration or background actions triggered Postgres error `null value in column "changed_by" of relation "appointment_history" violates not-null constraint` because `auth.uid()` evaluates to `NULL` in trigger context | Declare unavailability or update appointment via service role | Database trigger logs status change into `appointment_history` without throwing NOT NULL constraint error | Postgres trigger function `log_appointment_history()` inserted `auth.uid()` directly into `changed_by`; when `auth.uid()` was NULL, query aborted with constraint error | Created migration `20260830182000_fix_log_appointment_history_null_auth_uid.sql` updating `log_appointment_history()` function to use `v_changed_by := COALESCE(auth.uid(), (SELECT id FROM users WHERE role = 'admin' ORDER BY created_at ASC LIMIT 1))`. Applied trigger update to database. | 2026-08-30 18:20 |
| BUG-061 | 2026-08-30 18:22 | 9 | P2-10 | 🟠 High | ✅ Fixed | Dentists and Reception staff could not delete/remove declared leave blocks from `dentist_blocks` because RLS policy `dentist_blocks_delete` only permitted `admin` role | Log in as Dentist (`dentist@clinic.local`), view `/dentists/unavailability`, click "Remove Block" | Leave block is deleted cleanly from database and removed from card list | RLS policy `dentist_blocks_delete` checked `get_user_role() = 'admin'`, blocking dentists and reception from removing leave blocks | Created migration `20260830182500_fix_dentist_blocks_delete_rls.sql` — updated `dentist_blocks_delete` policy to allow `dentist_id = get_current_dentist_id() OR get_user_role() IN ('admin', 'reception')`. Applied migration to database. | 2026-08-30 18:23 |
| BUG-062 | 2026-08-30 18:24 | 9 | P2-11 | 🟠 High | 🐛 Open | Rescheduling patient appointments requires further review & testing | Open `/dentists/unavailability`, declare leave, click Reassign Dentist | Patient appointment rescheduled to new doctor & slot cleanly | Rescheduling patient appointments still not working as expected | Logged issue for documentation per user instruction. Code untouched, documented as open item for future review. | N/A |
| BUG-063 | 2026-08-31 17:15 | 9 | P2-09 | 🔴 Critical | ✅ Fixed | Timezone bug: `new Date().toISOString().split("T")[0]` converts to UTC first, shifting the date back by one day in UTC+8 (Philippine time). Appointments on the current or last day of the month were excluded from calendar, check-in, queue, dashboard stats, and other date-filtered queries. | In UTC+8 timezone, view appointments calendar for current month — last day appointments missing. Check-in page shows "No appointments scheduled for today" even when appointments exist. | Appointments appear on correct dates in calendar, check-in, queue, and dashboard | Appointments on boundary dates (today, last day of month) are excluded because `toISOString()` converts local 5pm to UTC (previous day) | Created `src/lib/utils/date-utils.ts` with `todayLocal()`, `formatDateLocal()`, and `endOfMonthLocal()` functions that format dates in local timezone. Replaced all 15+ instances of `new Date().toISOString().split("T")[0]` across server pages, API routes, and client components with these utility functions. Files updated: `appointments/page.tsx`, `check-in/page.tsx`, `consultation/page.tsx`, `dashboard/actions.ts`, `queue/page.tsx`, `queue/actions.ts`, `dentist-portal/emergency/actions.ts`, `dentist-portal/more/page.tsx`, `dentist-portal/queue/page.tsx`, `dentist-portal/schedule/page.tsx`, `api/check-in/search/route.ts`, `api/queue/route.ts`, `api/cron/reminders/route.ts`, `appointment-calendar.tsx`, `check-in-client.tsx`, `waitlist-client.tsx`, `appointment-form.tsx`, `follow-up-scheduler.tsx`. | 2026-08-31 17:25 |
| BUG-064 | 2026-08-31 17:35 | 9 | P2-08 | 🟠 High | ✅ Fixed | Bookings dashboard "All Requests" tab shows 0 results — client component uses `useState(initialBookings)` which only initializes once with pending-only server data. Tab clicks only filter client-side without fetching new data from server. | Open `/bookings`, click "All Requests" tab | All appointments (including rescheduled) appear in the list | "No Booking Requests Found" with 0 count — server only fetched `pending` appointments, client-side filtering can't show what was never fetched | Three fixes: ① Added `"all"` to `validStatuses` array in `bookings/page.tsx` so server recognizes `?status=all` param. ② Changed tab `onClick` in `booking-dashboard-client.tsx` to call `router.push('/bookings?status=${f.key}')` for server-side data fetch per filter. ③ Added `key={filterStatus}` prop to `BookingDashboardClient` to force remount with fresh server data on filter change. Also fixed "View All Requests" button in empty state to navigate to `?status=all`. | 2026-08-31 17:40 |
| BUG-065 | 2026-08-31 20:00 | 9 | P3-02 | 🟠 High | ✅ Fixed | QR code generation fails with `new row violates row-level security policy for table "qr_codes"` when a dentist generates a QR code for patient registration. FR-23 says "staff" can generate QR codes, but RLS `qr_codes_insert` policy only allowed `reception` and `admin` roles. | Log in as dentist, navigate to `/check-in/qr/[appointmentId]`, click "Generate QR Code" | QR code generates successfully with token and countdown timer | Postgres RLS error: `new row violates row-level security policy for table "qr_codes"` — insert denied because `get_user_role()` returns `'dentist'` which is not in the policy's allowed roles | Created migration `20260901000000_fix_qr_codes_rls_for_all_staff.sql` — dropped and recreated `qr_codes_insert` and `qr_codes_update` policies to include `'dentist'` alongside `'admin'` and `'reception'`. Applied via `npx supabase db reset --local`. | 2026-08-31 20:00 |
| BUG-066 | 2026-08-31 19:55 | 9 | P2-02 | 🔴 Critical | ✅ Fixed | "Maximum update depth exceeded" error in `patient-form-dialog.tsx:57` — `useEffect` dependency array included `conditionIds` (an array prop defaulting to `[]`), creating a new reference on every render and triggering an infinite loop of `setState` calls via `reset()`. This also caused the "Dental History" and "Medical History" tabs to be unclickable (UI frozen by infinite re-renders). | Open patient registration dialog (`/patients` → "Register New Patient" or "Edit Demographics"), observe console | Dialog opens, tabs are clickable, no console errors | Console floods with "Maximum update depth exceeded" errors, dialog UI is frozen, tabs (Dental History, Medical History) do not respond to clicks | Added `useRef(prevOpen)` to track the previous `open` prop state. Modified `useEffect` to only execute `reset()` logic when `open` transitions from `false` to `true`, preventing re-runs while the dialog is open. File: `src/components/patients/patient-form-dialog.tsx` lines 55-63. | 2026-08-31 19:55 |
| BUG-067 | 2026-08-31 19:55 | 9 | P3-03 | 🟠 High | ✅ Fixed | QR code patient registration (`/register/[token]`) only captured 6 basic fields (first_name, last_name, contact_no, email, birth_date, medical_history, allergies) — missing all dental history, medical questions, allergy checklist, and medical conditions fields that the staff registration form captures. | Scan QR code, fill out registration form | All patient data fields captured (personal info, dental history, medical history, allergies, conditions) matching staff registration | Only 6 basic fields captured — no dental history, no medical questions, no allergy checklist, no medical conditions | Rewrote `registration-wizard.tsx` to use `react-hook-form` with `patientSchema` and shared form section components (`PatientFormPersonalSection`, `PatientFormDentalHistorySection`, `PatientFormMedicalQuestionsSection`, `PatientFormAllergyConditionsSection`). Added 4-tab layout (Personal Info, Dental History, Medical History, Review) matching staff registration. Updated `actions.ts` `submitRegistrationAction` to handle all patient schema fields. Added `getMedicalConditionsAction` to fetch conditions. Updated `page.tsx` to fetch and pass conditions. Files: `src/app/(public)/register/[token]/registration-wizard.tsx`, `src/app/(public)/register/[token]/actions.ts`, `src/app/(public)/register/[token]/page.tsx`. | 2026-08-31 19:55 |

- **Status:** 🐛/🔄/✅/❌
- **Date Found:** YYYY-MM-DD HH:mm UTC+08:00
- **Date Fixed:** YYYY-MM-DD HH:mm UTC+08:00 (or "N/A")

**Description:**
[Detailed description of the bug]

**Steps to Reproduce:**
1. [Step 1]
2. [Step 2]
3. [Step 3]

**Expected Behavior:**
[What should happen]

**Actual Behavior:**
[What actually happens]

**Root Cause:**
[Why the bug occurs]

**Fix:**
[What was changed to fix it — file(s), line(s), approach]

**Regression Test:**
[How to verify the fix doesn't break anything else]
-->

---

## Summary Statistics

| Severity | Open | In Progress | Fixed | Won't Fix | Total |
|---|---|---|---|---|---|
| 🔴 Critical | 1 | 0 | 6 | 0 | 7 |
| 🟠 High | 0 | 0 | 17 | 0 | 17 |
| 🟡 Medium | 0 | 0 | 16 | 0 | 16 |
| 🟢 Low | 0 | 0 | 8 | 0 | 8 |
| **Total** | **1** | **0** | **47** | **0** | **48** |
